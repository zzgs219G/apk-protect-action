# reasonix session

## User

阅读流程文档，我想开发一个dex的一个字符串加密（我说的字符串就是那种源码中写的那个字符串，而不是方法名，类名这些，还有那种系统类，还有那种依赖类的那些字符串）

## Assistant

现在我理解了关键点。dcc 在转译 `const-string` 时，直接生成 `env->NewStringUTF("明文")` —— 字符串原文会明晃晃地出现在生成的 C++ 代码里。这正是你要加密的目标。

让我再看看 dcc 的 filter 机制和整体流程，以及 compiled_methods 的生成方式，搞清楚字符串加密应该接入哪一层。

现在我对整个系统已经有了充分理解。让我确认一下你现有的两个真实 APK 用于测试，以及查看流程文档中关于 dex2c/字符串相关的历史，然后给出方案。

我已经读完了核心流程文档和关键代码。先总结一下我理解的情况，然后给你方案设计，因为这是一个需要你拍板的设计决策（不是直接开写代码）。

---

## 你要加密的字符串是什么

你说的「源码里写的字符串」= dex 里的**字符串常量**，也就是 smali 反编译后那些 `const-string v0, "xxxxx"` 里面的明文。例如：

- 硬编码的 URL / API key / token / 密钥
- 日志文案、错误提示
- 特定业务标记、加密盐等

**不是**方法名、类名、系统类、依赖类的字符串。这些排除项很重要，因为它们属于 dex 的「引用」结构（type_id/string_id 被 class 定义、方法签名引用），动了会破坏 dex 结构语义，导致无法加载或闪退。你要保护的是「字符串常量本身」，即源码里写死的那批。

---

## 现在的关键事实（我调研代码后确认）

你的加固流水线里已经有一个 **Dex2C** 模块，它把选中的方法（由 filter 决定）从 smali 翻译成 C 代码编进 `libnc.so`。而 dcc 翻译 `const-string` 时，生成了这样一行：

```c
vXXX = (jstring) env->NewStringUTF("明文字符串");
```

**这意味着：目前被 Dex2C 抽走的方法里的字符串常量，会以明文形式出现在生成的 `.cpp` 里**（`writer.py` 的 `visit_load_constant`）。而那些**没被抽走**、留在 smali 里的方法，它们的字符串在 dex 里本身就是明文（dex string_data 区域，`strings` 就能看到，更别说反编译）。

所以「字符串加密」在这个仓库里天然可以分两层做，难点和收益完全不同。

---

## 我的方案建议（需要你拍板范围）

我想确认你要做哪种，这决定了改动量和风险：

