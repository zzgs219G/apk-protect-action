# apk-protect-action 流程文档(AI 可执行 / 可审查版)

> 本文是**事实性流程文档**,不是设计讨论。任何 AI(人或流水线)接手本项目时,
> 以本文为准。文档中每个步骤都标注了【输入】【操作】【产出】【审查点】,
> AI 审查者应逐条核对,不得跳步。
>
> 最后更新:随 `scripts/pipelines/protect.sh` / 三个 filter 脚本一同维护。

---

## 0. 系统总览

### 0.1 流水线与模块(共用一套工具,不要混淆)

**唯一总控:`scripts/pipelines/protect.sh`**(2025-09 起取代旧三个总控
`protect-sigcheck.sh` / `protect-dcc.sh` / `protect-sigcheck-only.sh`
与编排器 `run-modules.sh`。旧三个 yml 已删除,唯一入口是
`protect-modules.yml` 勾选式 workflow)。

| | 只勾 sigcheck | 只勾 dex2c | sigcheck + dex2c 都勾 |
|---|---|---|---|
| 类名来源 | 无需类名(只插主 Activity) | 手动:用户输入规则 | 自动:Manifest 解析 LAUNCHER |
| dex2c 转译 | **无** | 有 | 有 |
| 额外注入 | `sig_check.c` + `sig_hash.h` | 无 | `sig_check.c` + `sig_hash.h`(并入 dcc 工程) |
| smali 处理 | `inject-loadlib.py` 双保险插桩 | `mark-native.py` 壳替换 | `mark-native.py` 壳替换(不插双保险) |
| 解包/回编次数 | 1 次 | 1 次 | **1 次**(旧联合方案 2 次,合并后收益) |
| 用途 | 单独测试签名校验模块 | 只做指令抽取 | 完整加固 |
| 产物 | 未签名 APK | 未签名 APK | 未签名 APK |

> **字符串加密(`--stringenc`)是与之正交的第四个模块**:dex 层,不碰 so,任意组合。
> 详见 §3.7。

执行顺序固定:sigcheck → dex2c → stringenc → packer(占位未接入,勾了直接报错),
与勾选顺序无关。

> **模块化路线**:以上三种勾选组合覆盖全部场景,共用一次解包/回编。已为此
> 打下的基础:
> - `scripts/inject/inject-loadlib.py`:loadLibrary 插桩的**可复用基础模块**
>   (幂等,多模块共存安全)
> - **so 名统一约定为 `nc`**:两模块都勾选时共享同一个 libnc.so
>   (dex2c 的 native 壳 + sig_check.c 的 constructor 并入同一 jni 工程,
>   constructor 先于 JNI_OnLoad 执行,天然兼容)
> - 单独签名校验的 NDK 工程用自包含 Android.mk(wildcard 收 `*.c` + `*.cpp`;
>   dcc 原版只收 `*.cpp`)

### 0.2 目录结构(必须与本文描述一致)

```
apk-protect-action/
├── .github/workflows/
│   └── protect-modules.yml       # 唯一入口:勾选式 workflow(旧三个 yml 已删)
├── scripts/                          # 按功能分目录,文件名=英文动宾短语
│   ├── inject/                       # 【基础模块】多场景共用的 smali 插桩
│   │   └── inject-loadlib.py         #   loadLibrary 插桩(幂等)+ LAUNCHER 查找 + CLI
│   ├── pipelines/
│   │   └── protect.sh                #   唯一总控(勾选式:sigcheck/dex2c/stringenc/envcheck/packer)
│   ├── sig-hash/                     # 证书指纹提取
│   │   ├── make-sig-hash.sh          #   步骤:提取证书指纹 → sig_hash.h
│   │   └── extract-cert-fp.py        #   纯 stdlib 解析 Signing Block 取证书 DER
│   ├── filter/                       # dcc filter 生成
│   │   ├── make-filter-from-apk.py   #   解析 Manifest → filter(联合勾选用)
│   │   └── rules-to-filter.py        #   用户通配符规则 → filter(只勾 dex2c 用)
│   ├── string-enc/                   # 【模块】dex 层字符串常量加密
│   │   └── encrypt-strings.py        #   规则选类 → 异或加密 → 注入 Java 解密桩
│   ├── sig-log/                      # 中文流程日志(密文形态)
│   │   └── make-sig-log.py           #   扫 LSG 注释 → sig_log_data.h
│   ├── lib/                          # shell 公共库(source 使用)
│   │   ├── common.sh                 #   ensure_dcc / normalize_path / 日志
│   │   ├── lib-ndk.sh                #   ndk_write_mk 唯一 mk 模板(报错十一/十三)
│   │   └── lib-repack.sh             #   解包/放 so/回编(报错五/八/十八)
│   └── repack/                       # 解包回填重打包
│       ├── mark-native.py            #   smali native 壳替换 + loadLibrary 插桩
│       ├── patch-extractnativelibs.py#   二进制 AXML 原位改写 extractNativeLibs(报错五)
│       └── patch-dex-version.py      #   dex 版本头 save/restore(报错十八)
├── sigcheck/
│   ├── src/sig_check.c           # 纯 C 签名校验(constructor 入口,非 JNI_OnLoad)
│   ├── src/env_check.c           # 环境检测(Frida/Xposed/调试器,--envcheck)
│   └── src/sig_hash.h            # 指纹派生头(全零占位;流水线生成后覆盖)
├── tools/
│   ├── dcc.zip                   # dcc 工具分发包(含内置 androguard/apktool,勿升级勿替换)
│   └── dpt-shell-v2.19.0.zip
```

### 0.3 核心事实速查(AI 审查前必须背下,历史上 3 个 bug 全栽在这里)

1. **dcc 的 `full_name` 没有 `->`**。
   `dcc.py` 用 `''.join(method_triple)` 拼接,结果是
   `Lcom/demo/app/MainActivity;onCreate(Landroid/os/Bundle;)V`
   ——分号后**直接跟方法名**,没有箭头。filter 里写 `->onCreate` 永远匹配不上。
2. **full_name 的类名是 smali 斜杠格式**。
   dex 描述符原样返回:`Lcom/demo/app/Main;`,**不是** `Lcom.demo.app.Main;`。
   用户输入和 Manifest 都是点号 → 写 filter 前必须 `点号 → 斜杠`。
3. **dcc 原版 native 化不完整**。
   `dcc.py` 只在自带重打包路径(不带 `--project-archive`)里替换 native 壳,
   且**从不插入** `System.loadLibrary`。我们用 `--no-build --project-archive`
   只取工程包,所以 native 化必须由 `mark-native.py` 自己做,否则必闪退
   (UnsatisfiedLinkError: 方法已声明 native 但 so 没加载 / 或方法体丢失)。

---

## 1. 前置依赖(workflow 已自动安装,审查时核对版本)

| 依赖 | 用途 | 安装位置 |
|---|---|---|
| JDK 17 (temurin) | apktool / java | `actions/setup-java@v4` |
| Android NDK r26d | ndk-build 编译 libnc.so | `nttld/setup-ndk@v1`,`ANDROID_NDK_HOME` 环境变量 |
| python3 + `build/dcc/dcc/requirements.txt`(自 `tools/dcc.zip` 解压,workflow 内置解压步骤) | dcc / androguard | `pip3 install -r` |
| apktool.jar 3.0.3 | 解包/重打包 APK | 随 `tools/dcc.zip` 携带,解压于 `build/dcc/dcc/tools/apktool.jar`(2.4.0→2.9.3→3.0.3,升级须回归 §5 灰包红线) |
| R2 Secrets | 上传产物 | `R2_ACCESS_KEY/R2_SECRET_KEY/R2_ENDPOINT/R2_BUCKET/R2_PUBLIC_DOMAIN` |

---

## 2. 联合方案流程(protect-modules.yml 勾选 sigcheck+dex2c → protect.sh)

> 输入:`workflow_dispatch.apk_url`(开发者 keystore 已签名的 APK)
> 输出:`protected_unsigned.apk`(未签名,用户需自行重签)

### 步骤 1:下载 APK

- 【输入】`apk_url`
- 【操作】`curl -fL --retry 3 -o app.apk "$apk_url"`,`ls -la` 确认落盘
- 【产出】工作目录 `app.apk`
- 【审查点】文件大小 > 0;`ls` 输出必须 `tee -a build.log`(否则失败无法排查)

### 步骤 2:提取证书指纹(make-sig-hash.sh → sig_hash.h)

- 【输入】`app.apk`
- 【操作】调 `extract-cert-fp.py`:纯 stdlib 解析 APK Signing Block
  (v3 `0xF05368C0` 优先,退 v2 `0x7109871A`),取第一张证书 DER → SHA-256
- 【产出】`sig_hash.h`,内容形如 `#define SIG_HASH ...`(64 hex 拆字节)
- 【审查点】
  - 指纹必须与 **最终发布重签** 的 keystore 一致(加固包重签后指纹会变!
    流程产出的包未签名,开发者重签后指纹才生效——若重签用不同 keystore,
    校验必然失败,这是设计约定不是 bug)
  - 指纹全零 = 调试模式,sig_check.c 会跳过校验(仅本地测试允许)

### 步骤 3:动态解析 Manifest → 自动 filter(联合方案核心)

- 【输入】`app.apk`
- 【操作】依次执行:
  1. `make-filter-from-apk.py app.apk $WORK/auto_filter.txt \
      --classes $WORK/activity_classes.txt --on-fail error`
     - 用 dcc 内置 androguard 读 AndroidManifest.xml
     - 取带 `android.intent.action.MAIN` **且** `android.intent.category.LAUNCHER`
       的 activity(enabled=false 的跳过)
     - 相对类名补全:`.MainActivity` → `com.pkg.MainActivity`;
       无点短名 `MainActivity` → `com.pkg.MainActivity`
     - **点号 → 斜杠**后写入 filter:`Lcom/pkg/MainActivity;.*`
     - `--classes` 同时导出全部 Activity 列表(供 activity* 展开)
     - 失败策略:解析不到 LAUNCHER → exit 2,流水线终止
       (报错信息提示用户改用单独 dcc 流水线手动指定类名)
- 【产出】`$WORK/auto_filter.txt`(已是 dcc filter 正则格式,直接使用)
- 【审查点】
  - filter 行必须是**斜杠**格式,出现点号即 bug
  - 出现 `->` 即 bug(见 0.3 第 1 条)
  - 多个 LAUNCHER 时全部纳入(日志会打印数量)
  - **禁止把 auto_filter.txt 再喂给 rules-to-filter.py 做二次转换**:
    该脚本输入是点号格式用户规则,二次转换会产生畸形正则导致
    `no compiled methods`(历史上真实发生过的 bug)

### 步骤 4:dcc 转译

- 【输入】`app.apk` + `auto_filter.txt`
- 【操作】
  ```bash
  cd $DCC_DIR   # dcc 目录
  python3 dcc.py "$IN_APK" \
    --project-archive "$WORK/dcc-project.zip" \
    --no-build \
    --filter "$WORK/auto_filter.txt"
  ```
- 【产出】`$WORK/dcc-project.zip`(jni 工程,内含生成的 .cpp 与
  `jni/nc/compiled_methods.txt`)
- 【审查点】
  - **`IN_APK` 必须是绝对路径**。`protect.sh` 开头已做
    `cd dirname && pwd` 规范化——因为本步骤 `cd $DCC_DIR` 后相对路径
    `app.apk` 会失效,这正是历史"报错二"的根因
  - `dcc.py` 出错时只 `logger.error` **不返回非零退出码**,所以必须紧跟
    `[[ -f "$WORK/dcc-project.zip" ]] || exit 1`,不能省
  - `compiled_methods.txt` 每行格式:
    `Lcom/pkg/Class;methodName(args)ret`(无箭头,斜杠类名)

### 步骤 5:合入签名校验代码

- 【操作】
  1. `unzip dcc-project.zip -d $WORK/project`
  2. `cp sigcheck/src/sig_check.c $WORK/project/jni/nc/`
  3. `cp $WORK/sig_hash.h $WORK/project/jni/nc/`
  4. 覆写 `jni/Application.mk`:`APP_ABI := armeabi-v7a arm64-v8a`
     (dcc 默认只有 v7a,必须双 ABI)
- 【审查点】
  - `sig_check.c` 用 `__attribute__((constructor))` 做入口,与 dcc 模板
    `Dex2C.cpp` 里的 `JNI_OnLoad` **不冲突**(dlopen 时 constructor 先于
    JNI_OnLoad 执行)。审查时若有人把 sig_check 改成 JNI_OnLoad 会链接冲突
  - Android.mk 用 wildcard 收 `jni/nc/*.cpp`,新加的 .cpp 自动进编译,无需改 mk

### 步骤 6:NDK 编译

- 【操作】`ndk-build -j$(nproc) -C $WORK/project`
  (PATH 前置 `$ANDROID_NDK_HOME`)
- 【产出】`$WORK/project/libs/armeabi-v7a/libnc.so`、
  `$WORK/project/libs/arm64-v8a/libnc.so`
- 【审查点】两个 ABI 目录都要有 so,缺一个说明 Application.mk 未生效

### 步骤 7:apktool 解包

- 【操作】`java -jar apktool.jar d -r -f -o $WORK/decompiled app.apk`
  (`-r` 不反编译资源,保持资源二进制一致;**`--force-manifest` 必须不带**,
  原因见下方"报错五":带上时顶层 Manifest 被解码成文本 XML,
  `apktool b` 在 `-r` 模式下会把文本 Manifest **原样拷回** APK、不重编成
  AXML,产物是系统/文件管理器都不认的"灰包"。
  历史"报错四"曾要求带 `--force-manifest`,那是文本 Manifest 消费方案的
  遗留约定;现 `inject-loadlib.py` 已走 androguard AXMLPrinter 解析二进制,
  `extractNativeLibs` 走 `patch-extractnativelibs.py` 原位补丁,不再依赖文本)
- 【审查点】解包目录下应有 `smali/`、`smali_classes2/`…(多 dex 时);
  顶层 `AndroidManifest.xml` 必须是**二进制**(头 4 字节 `03 00 08 00`),
  若是文本 XML 立即终止排查

### 步骤 8:smali native 化(mark-native.py,不可跳过)

- 【输入】`$WORK/project/jni/nc/compiled_methods.txt` + `$WORK/decompiled`
- 【操作】对 compiled_methods.txt 中每个 (类,方法,原型):
  1. 在对应 .smali 里找到 `.method ... name(proto)`,把整个方法体替换为
     `.method native ... name(proto)`(native 方法不能有方法体)
  2. 每个被改的类插入 so 加载:
     - 已有 `<clinit>` → 在其开头插
       `const-string v0, "nc"` +
       `invoke-static {v0}, Ljava/lang/System;->loadLibrary(Ljava/lang/String;)V`
     - 没有 `<clinit>` → 在 `.class` 行后新建一个
  3. 类里已有 loadLibrary 则跳过(幂等)
- 【产出】修改后的 smali;stdout 打印每个文件的替换数
- 【审查点】
  - **退出码 3 = 有方法没在 smali 里找到** → 流水线会直接失败(set -e),
    必须排查(可能是混淆重命名/多 dex 路径问题),不许静默忽略
  - 幂等性:重复运行会再次匹配 native 头并原样重写,输出不变,安全

### 步骤 9:放 so

- 【操作】把 `$WORK/project/libs/*/libnc.so` 复制到
  `$WORK/decompiled/lib/<abi>/libnc.so`
- 【审查点】abi 目录名必须是 `armeabi-v7a` / `arm64-v8a` 原名

### 步骤 10:extractNativeLibs 强制 true

- 【操作】`python3 scripts/repack/patch-extractnativelibs.py $WORK/decompiled/AndroidManifest.xml`
  —— 直接解析**二进制 AXML**,把 `<application>` 的 BOOLEAN 属性
  `extractNativeLibs` 在原位从 false(0x12/data=0)改写为
  true(0x12/data=0xFFFFFFFF),等尺寸、大小与偏移全不变。
  (报错五之前用 sed 改文本 Manifest;现在 Manifest 全程保持二进制,无法 sed)
- 【审查点】这是**唯一允许的 Manifest 修改**,其他一律不许动
  (方案红线:不改 Application 类名、不加组件);
  脚本幂等(已是 true 跳过);属性不存在或非 AXML 时**报错退出**而非静默

### 步骤 11:重打包 + zipalign

- 【操作】`apktool b -o $WORK/unsigned.apk $WORK/decompiled`;
  有 zipalign 则 `zipalign -f 4`,没有则直接 cp 并打警告
- 【审查点】**全程不签名**。产物必须未签名,签名是开发者重签的责任

### 步骤 12:上传 R2 + Artifact 备用

- 【操作】`aws s3 cp` 到 `s3://$R2_BUCKET/apks/${GITHUB_RUN_ID}_sigcheck包.apk`;
  同时 `upload-artifact`(Artifact 是 R2 失败时的兜底)
- 【审查点】下载链接拼接 `$R2_PUBLIC_DOMAIN/apks/<name>`,
  notice 用 `::notice` 输出到 Actions 摘要

---

## 3. 单独 dcc 流程(protect-modules.yml 只勾 dex2c → protect.sh)

与联合勾选的差异(其余步骤 4~12 完全相同,审查标准同上):

| 差异点 | 联合勾选 | 只勾 dex2c |
|---|---|---|
| 步骤 2(指纹) | 有 | **无** |
| 步骤 3(找类) | Manifest 自动解析 | 用户输入 `dex2c_rules.txt` |
| 步骤 5(合入校验代码) | 有 | **无**(不拷 sig_check.c / sig_hash.h) |
| Application.mk | 流水线覆写双 ABI | 流水线覆写双 ABI(已统一) |

### 3.1 类名规则文件格式(`workflow_dispatch.class_rules`,一行一个类)

| 规则 | 语义 | 生成 filter(斜杠格式) |
|---|---|---|
| `com.test.**` | test 包所有类,含子包 | `Lcom/test/.*;.*` |
| `com.test.*` | test 包所有类,不含子包 | `Lcom/test/[^.]*(/.+)?;.*` |
| `com.test**` | 等价 `com.test.**` | 同上第一行 |
| `com.test*` | com 下 test 开头的所有类(testx、test 均命中,可带子包) | `Lcom/test[^.]*(/.+)?;.*` |
| `com.app.MainActivity` | 精确类名 | `Lcom/app/MainActivity;.*` |
| `activity*` | 所有 Activity 子类(从 Manifest 提取) | 逐类展开为具体行 |
| `!com.test.libs.**` | 排除(优先) | `!Lcom/test/libs/.*;.*` |
| `# 注释` / 空行 | 跳过 | — |

- 【审查点】
  - `activity*` 检测用的是 `grep '^[[:space:]]*activity\*[[:space:]]*$'`,
    **不要改回 `\s`**(grep BRE 不认,历史 bug)
  - `activity*` 展开依赖 `--classes` 类列表;类列表兼容点号
    (`com.a.B # activity`)与 smali(`Lcom/a/B;`)两种格式
  - 精确类名不会误伤 `MainActivityHelper`(有分号锚定)

### 3.2 执行顺序细节

`protect.sh` 步骤 2 **先**跑 `rules-to-filter.py`,**再**检测是否含
`activity*`,含则补跑 `make-filter-from-apk.py --on-fail skip` 生成类列表。
注意:若规则含 `activity*` 但解析失败(skip),filter 里 `activity*` 会展开为
空 → dcc 抽不到方法 → 工程包不存在检查处失败。审查时看日志里
"待 native 化方法: 0 个" 即为此场景。

---

## 3.5 单独签名校验流程(protect-modules.yml 只勾 sigcheck → protect.sh)

> 模块化流水线:只注入签名校验,**不跑 dex2c**。
> 用途:单独测试签名校验模块、单独定位问题。所有"模块化基础约定"见 §0.1 末尾的说明块。

与联合勾选的差异(其余步骤 6~12 完全相同,审查标准同上):

| 差异点 | 联合勾选 | 单独签名校验 |
|---|---|---|
| 步骤 3(找类) | Manifest 自动解析 → filter | **无**(不需要) |
| 步骤 4(dcc 转译) | 有 | **无**(跳过) |
| 步骤 5(合入校验代码) | 解包 dcc 工程后 cp 进 jni/nc/ | **组装最小 NDK 工程**:`sig_check.c` + `sig_hash.h` 放 jni/ 根;ndk_write_mk 统一模板两级 wildcard 都收(`*.c` + `*.cpp`,dcc 原版只收 `*.cpp`,sig_check.c 是 .c 所以必须收 .c) |
| 步骤 8(native 化) | mark-native.py | **无方法可 native 化**;改为 `inject-loadlib.py --launcher --entrypoints` 做**双保险插桩**:主 Activity 的 `<clinit>` **和** `onCreate` 各插一份 loadLibrary("nc")(App 启动即加载 so → 触发 constructor 校验)。LAUNCHER 从二进制 AXML 解析(androguard AXMLPrinter,报错五修复后 Manifest 不再是文本) |

- 【审查点】
  - so 名必须是 `nc`(模块化约定,与 dex2c 共享 so 的前提)
  - loadLibrary 插桩必须走 `inject-loadlib.py`(幂等按方法体独立检查)
  - **双保险插桩(单独方案专属)**:`<clinit>` + `onCreate` 各一份,
    攻击者要删两处才能绕过校验。联合方案(dex2c)**不要**用:
    onCreate 已抽进 so 成 native 壳,删 `<clinit>` 的 loadLibrary 会让
    native 壳 `UnsatisfiedLinkError` 自爆,天然无双保险需求
  - **`<clinit>` 永远不能被 dex2c 抽进 so**(filter 的 `!<clinit|init>`
    排除规则):被抽进 so 的方法要先加载 so 才能执行,而 loadLibrary
    就在 `<clinit>` 里 —— 鸡生蛋。dex 必须永远保留一条 Java 层
    loadLibrary,这是防线不可删除的根本约束
  - onCreate 无法插桩的情形(类未 override / onCreate 零局部寄存器,
    后者给 `.registers` +1 会让参数寄存器编号平移、方法体引用全错位,
    宁可不插):`<clinit>` 那份仍在,校验不受影响;inject-loadlib 会
    打印状态(no-method / no-local-register),流水线日志可见
  - 校验时机:主 Activity 的 `<clinit>` 在 App 启动时执行 → loadLibrary
    → so 的 constructor 立即校验签名,早于一切业务代码;onCreate 里
    那份是 `already` 跳过(so 已加载,重复加载无害)

---

## 3.7 字符串加密模块(protect.sh --stringenc 规则文件)

> **定位**:dex 层的字符串常量加密,与 so 完全无关(不碰 dcc / libnc.so / native 层),
> 属于与 dex2c 并列的**独立模块**,可单独勾选,也可与任意模块组合。
> 用途:让 `strings` / 静态特征扫描看不到源码里写死的明文(URL / API key / 日志文案等)。

### 3.7.1 加密对象与边界(审查时最容易搞错的地方)

| 对象 | 是否加密 | 说明 |
|---|---|---|
| `const-string` / `const-string/jumbo` 字面量 | ✅ | 指令级改写,见 3.7.2 |
| **字段的字符串值**(`static final String S = "x"`) | ✅ | R8 会把它折叠成 `.field ... = "x"`(**不是** `const-string` 指令!),由 field 分支单独处理 |
| `<clinit>` 里 `const-string` + `sput-object` 的字段赋值 | ✅ | 形态上就是普通 `const-string` 指令,天然覆盖 |
| 字符串数组元素 / 枚举参数 / 接口常量 | ✅ | 编译产物同样是 `const-string`(数组元素为 `aput-object` 前的字面量) |
| 字段**名** / 方法名 / 类名 / 类型描述符 | ❌ | dex 结构引用(type_ids/field_ids/method_ids→string_id),动了类加载即崩 |
| 注解值(`@Foo(value = "x")`) | ❌ | 不是 `const-string` 指令,天然不在扫描面 |
| Manifest / 资源里的字符串 | ❌ | 不在 dex 里,本模块不涉及 |

- **加密粒度 = 指令级字面量**,不做"字段值密文化"那种存储层改写(要破坏常量传播、
  改写所有已内联点、给反射读取路径补解密,收益却与指令级几乎相同)
- **安全边界(必须对用户讲明)**:这是**防静态可见性**,不是密码学安全。密钥流派生
  材料(salt)与密文同处一个 dex,懂格式者仍可解。真正的密钥材料须走服务端方案

### 3.7.2 指令改写形态(不新增寄存器)

```
改前: const-string v0, "明文"
改后: const-string v0, "<Base64 密文>"
      invoke-static/range {v0 .. v0}, Lcom/nc/strdec/StrDec;->d(Ljava/lang/String;)Ljava/lang/String;
      move-result-object v0
```

- **参数与结果都用原寄存器 `v0`** → **不需要 `.registers +1`**,
  从根上规避报错十一/十三那类寄存器错位问题(这是本模块最重要的实现约束)
- **注入的 invoke 一律用 `/range` 形态**:`invoke-*` 是 35c 格式,寄存器列表
  仅 4 位(**上限 v15**),而 `const-string`(21c)可用到 v255 → 目标寄存器
  编号 ≥16(Compose/Kotlin 大方法,或 `pN` 且换算后 ≥16)时 `{vN}` 会直接汇编
  失败(报错二十一)。单寄存器 range 恒连续、dex 中同为 3 个 code unit(无体积
  代价),且对 `vN`/`pN` 都合法 → **不需要**解析 `.registers`/`.locals` 做换算;
  幂等识别正则 `_PAYLOAD_RE` 必须同时认 `invoke-static {..}` 与
  `invoke-static/range {.. .. ..}` 两种形态(否则续跑 `max_existing_idx` 漏算
  → keystream 复用)
- 字段常量(`.field ... = "明文"`)的初值**保持原字面量不动**(报错二十四:
  旧版曾把初值改写成 `= Lcom/nc/strdec/StrDec;->F000000:` 引用形态,
  被汇编成 dex 规范不允许的 FIELD 型(0x19) encoded static_value → ART
  类初始化阶段秒闪退且无日志)。加密改由**回填**实现:在字段所属类的
  `<clinit>` 末尾注入
  `sget-object vX, StrDec;->FNNNNNN` + `sput-object vX, <本类>;-><字段>`,
  运行期用解密结果覆盖字面量初值 —— 与桩自身 `<clinit>` 填 F 字段同一套
  合法形态。产物含字段常量时必须用 androguard 读回验证:FIELD 型(0x19)
  static_value 必须为 0(脚本已内置构建期断言 `_ILLEGAL_FIELD_INIT_RE`,
  复读全包发现非法形态即 fail-fast)
- 密文用 **Base64( idx_be24 || cipher )** 存成 ASCII,规避 `\u0000`/代理对/转义坑;
  代价是 dex 体积膨胀约 1.33×(仅限被加密的字符串)

### 3.7.3 算法(构建期与运行期必须逐字节一致)

```
salt      : 16 字节随机(os.urandom),构建期生成;重跑已有桩时复用其 SALT_B64
idx       : 本构建内该密文的唯一编号(3 字节大端,上限 16,777,215)
keystream : ks_block(j) = SHA-256( salt || idx_be24 || byte(j) )
cipher[i] = plain[i] ^ ks_block(i / 32)[i % 32]
```

- 属**轻量异或**(用户选定的强度档),但 key 流由 SHA-256 派生,优于固定短 key 轮转
- 两侧一致性靠**对拍测试**锁定(构建期 Python 实现 == 桩的 Java 实现),
  边界用例:长度 0 / 31 / 32 / 33(块内截断、跨块、满块)、UTF-8 多字节、NUL、emoji
- 历史教训同型:报错十八的"两侧派生协议必须以对拍为准,不能只看注释自以为一致"

### 3.7.4 解密桩(纯 Java,无 so 依赖)

```
类: Lcom/nc/strdec/StrDec;
  d(String) -> String   入口:查缓存 → dec() → intern() → 入缓存
  dec(String) -> String Base64 解码 → xor() → new String(UTF-8).intern()
  xor([B I) -> [B       密钥流生成与逐字节异或
  <clinit>              初始化缓存表 + 为字段常量填值
```

- **`intern()` 必须保留**:Java 字面量是同一 interned 实例,代码里 `==` 比较字符串
  可能依赖它 → 解密结果必须 `intern()`(并由缓存保证同一密文返回同一实例)
- **缓存表**(`ConcurrentHashMap`)保证 `==` / `switch` 语义不变
- 解密失败时 `catch` 返回原密文串,不抛异常(避免把"解密出问题"变成闪退)
- **桩自身在硬排除名单内**,永不被加密(否则递归)
- ⚠️ **`.registers` 必须 ≤ 16**:`array-length`/`int-to-byte` 是 12x 格式,
  寄存器上限 v15;`.registers 20` 会让参数 `p0` 落到 v18 → 汇编器报
  `Invalid register: v18. Must be between v0 and v15`
- ⚠️ **形参寄存器区不得当临时槽**(报错二十二,已由构建期自检锁死):
  `.registers 16` + `xor([BI)` ⇒ 参数固定落在末尾 `p0=v14`、`p1=v15`。
  内层循环若用 v14/v15 当异或临时值,等于改写形参本身 → 第 2 轮
  `aget-byte v14, v14, v13` 把 int 当数组引用 → ART **SIGSEGV**
  (非 null 非法引用不做检查直接解引用,Java `catch Throwable` 也拦不住)
  → 闪退且**无 FATAL 日志**(段错误不走 ART uncaught-exception 路径)。
  正确槽位:`v7`/`v8`(外层循环用完即死、每轮 `goto :loop_outer` 必重赋,
  内层期间无人读)。`_render_stub()` 的自检拒绝任何"写指令目标寄存器
  落入参数区"的桩,改错即构建期 fail-fast

### 3.7.5 选类规则(与 dex2c 完全同一套语法)

| 规则 | 语义 |
|---|---|
| `com.test.**` / `com.test.*` / `com.test*` | 同 §3.1 全部语义 |
| `com.app.MainActivity` | 精确类名 |
| `activity*` | 需 `--classes`(protect.sh 自动从 Manifest 提取) |
| `!com.test.libs.**` | 排除(优先级最高) |
| `# 注释` / 空行 | 跳过 |

- 复用 `rules-to-filter.py` 的 `class_rule_to_filter` / `has_wildcard` /
  `expand_activity_keyword`(**同一份实现,不另写一套**)
- ⚠️ **必须把「用户规则文件」直接交给 `encrypt-strings.py`**,
  绝不可先转成 dcc filter 再喂进去:那是二次转换,会产生畸形正则
  `LLcom/myapp//[^.]*;/[^.]*(/.+)?;.*` → 0 命中(与 §2 步骤 3 的 filter 坑同型)

### 3.7.6 硬排除名单(规则层不可突破)

即使用户写 `**` 全量,下列前缀的类一律不加密(用户明确要求):

```
android.  androidx.  android.support.  com.android.  com.google.android.
dalvik.   java.      javax.            kotlin.       kotlinx.
org.jetbrains.  org.apache.  org.json.  org.w3c.  org.xml.  sun.
```

外加解密桩自身 `com.nc.strdec`。用户 `!` 排除行优先级最高。
- 【审查点】前缀判定必须带**点号**(`android.` 而非 `android`)——
  否则 `androidXcustom` 这种同前缀类会被误排除
- 【审查点】日志会打印"被硬排除跳过: N 个类",为 0 且规则写了 `**` 时排查前缀表

### 3.7.7 接入点与执行顺序

- `protect.sh` STEP 5 内,**必须在 `mark-native.py` 之后**:
  被 dex2c 抽走的方法此时已是 native 壳(无方法体),字符串加密自然扫不到;
  同时用 `--exclude-methods compiled_methods.txt` 做**精确跳过**(防 dcc 与
  mark-native 之间的时序差异导致误加密,报错十三式的布局假设错误)
- 每轮先跑 `encrypt-strings.py clean <解包目录>`:按组件标记清理上一轮残留的
  解密桩(本次 0 条命中时不能把旧桩带进产物)
- **幂等**:已加密的类插了标记注释 `# nc-strdec-encrypted`(注释不进 dex),
  重跑整类跳过,输出逐字节稳定
- **组件标记**(`<解包目录>/stringenc-components.json`):记录"描述符 + 源文件路径",
  回编会重新分派 dex 号,这是唯一稳定的定位依据(报错十八式:信息在丢失前 save)
- 【审查点】`clean` 只删标记里登记的路径且要求文件含生成标记,
  绝不越界、绝不误删用户自己的类

### 3.7.8 与其余模块的组合

| 组合 | 行为 |
|---|---|
| 只勾 stringenc | 只加密 + 注入桩,不碰 so/签名校验 |
| sigcheck + stringenc | so 侧校验照常;dex 侧字符串加密,两者互不影响 |
| dex2c + stringenc | dex2c 抽走的类其字符串随方法进 so(被 `--exclude-methods` 跳过),未被抽走的类留在 dex 加密 |
| 三者全勾 | 共用**一次**解包/回编(解包/回编次数仍为 1) |

---

## 4. 关键格式规范(审查任何改动时先对表)
### 4.1 dcc full_name(方法指纹的唯一真相)

```
<class_name><method_name><proto_with_return_type>
例: Lcom/demo/app/MainActivity;onCreate(Landroid/os/Bundle;)V
     ^^^^^^^^^^^^^^^^^^^^^^^^^^^ ^^^^^^^^ ^^^^^^^^^^^^^^^^^^^^^^^
     smali 斜杠类名(含 L 与 ;)   方法名   参数原型含返回类型
```

- 拼接代码:`dcc.py compile_dex()` 中 `''.join(method_triple)`
- class_name 来源:dex 描述符(androguard 原样返回,斜杠)

### 4.2 compiled_methods.txt

- 位置:`dcc-project.zip → jni/nc/compiled_methods.txt`
- 每行一个 full_name(格式同 4.1),由 `write_compiled_methods()` 写入
- `mark-native.py` 用正则 `^(L[^;]+;)([^(]+)(\(.*\)\S+)$` 解析

### 4.3 JNI 函数名(生成 cpp 的函数头)

- `Java_<pkg斜杠转下划线>_<Class>_<method>__<proto参数mangle>`
- `.` 和 `/` → `_`;`_` → `_1`;`;` → `_2`;`[` → `_3`
- 函数靠 JNI 命名规范自动绑定,**不需要 RegisterNatives**
- 改类名/方法名会断绑定,审查时警惕

### 4.4 filter 文件语法

```
!<正则>   排除:命中则强制不转译(优先级最高)
=<全名>   精确匹配转译
<正则>    search 命中即转译
# 注释、空行跳过;首部固定加 !<clinit|init>
```

---

## 5. 报错对照表(AI 排障第一入口)

| 报错 | 根因 | 修复 |
|---|---|---|
| `ModuleNotFoundError: asn1crypto` | pip 依赖未装 | `pip3 install -r sigcheck/dex2c/dcc/requirements.txt`(已修,workflow 内置) |
| `file app.apk is not exists` + 后续"工程包不存在" | IN_APK 相对路径,`cd $DCC_DIR` 后失效 | 脚本开头绝对路径规范化(已修);审查新脚本是否有同样模式 |
| 工程包不存在但 dcc 无报错 | filter 未命中(类名写错/格式不匹配) | 按 §4.1 核对 full_name;确认无 `->`、类名是斜杠 |
| `UnsatisfiedLinkError` 运行时闪退 | native 化缺失或 so 未加载 | 检查步骤 8 是否执行、`<clinit>` 是否插入、extractNativeLibs 是否 true |
| `mark-native.py` exit 3 | 方法没在 smali 中找到 | 查多 dex 目录遍历(已修为匹配 `smali(_classesN)?` 层级);查混淆改名 |
| `未从 AndroidManifest.xml 找到 LAUNCHER activity`(sigcheck-only 步骤 4.5) | `inject-loadlib.py` 两条解析路径都失败:二进制 AXML 需要 androguard(随 dcc,`_DCC_DIR` 路径要对),文本 XML 用正则 | 确认 `_DCC_DIR` 路径与 dcc 内置 androguard 可导入;报错五修复后 Manifest 是二进制,走 AXMLPrinter 分支 |
| **报错六:`⚠️ androguard 不可用...No module named 'lxml'` + `未找到 LAUNCHER activity`(仅 sigcheck-only 云端)** | `protect-sigcheck-only.yml` 漏装 python 依赖(另两条 workflow 都有)→ runner 系统 python 无 lxml → AXMLPrinter 导入失败 → inject-loadlib 降级文本正则 → 解包目录 Manifest 保持二进制(报错五的正确行为),正则匹配必失败 | workflow 补 `pip3 install -r sigcheck/dex2c/dcc/requirements.txt`(已修);教训:**新增流水线 yml 时,python 依赖步骤是必抄项;`降级文本解析` 只对文本 XML 有效,二进制 AXML 场景下降级=必失败,不要误判成解包出了文本** |
| **报错九:`no compiled methods` → `dcc 未产出工程包`(联合方案/单独 dcc,仅多 dex 包;单 dex 包云端/本地均正常)** | dcc 原版 `auto_vm()` 用 `apk.APK(filename).get_dex()` 只读 `classes.dex`(androguard 文档明示多 dex 需用 `get_all_dex`,dcc 从未调用)→ `MainActivity` 在 `classes2.dex…classesN.dex` 的包里,dex 方法清单里根本没有目标类 → filter 再正确也 0 命中 → `dcc()` 静默 return(exit 0)→ 靠工程包存在性检查兜底报错。本地用单 dex 简盒 2.1.0 复现失败、签名校验单独流水线(不跑 dcc)正常,极易误判为 filter/环境问题 | dcc.py 新增 `auto_vm_all()`:APK 输入按 `get_dex_names()` 排序加载全部 dex 成 `DalvikVMFormat` 列表;`compile_dex()` 改为多 dex:`Analysis(vms[0])` + 逐个 `dx.add(vm)`(MethodAnalysis 注册到同一 Analysis,`get_source_method` 可跨 dex 检索),`MethodFilter` 的 conflict/native 检测按 dex 逐个补充(all_methods 局部变量,跨 dex 同名方法不互相误杀),主循环遍历所有 vm。验证:双 dex 伪造包(简盒 classes.dex+classes2.dex)全链路命中 3 方法产出工程包;单 dex 回归一致;`Analysis.methods` 68402 全覆盖。教训:**第三方工具"本地能跑"不等于"覆盖你的输入空间";多 dex 场景要用伪造多 dex 包专门验证;签名校验单独流水线通过恰好证明问题被隔离在 dcc 环节(模块化流水线的价值)** |
| **报错八:注入后 APK 膨胀约 380KB~1.8MB(sigcheck-only/联合方案,大包明显)** | apktool `baksmali → smali → 回编` 重建 dex 时,把 R8 release 构建原本精简的调试信息(行号表/局部变量表,即 dex `debug_info` 区块)重新生成回来,且按方法数量线性放大(实测简盒包:debug_info 63KB → 1.86MB,dex +1.80MB,APK +381KB)。R8 本身未被破坏:字符串池/type_ids/class_defs 逐项 ±0,代码区仅 +26 字节(插桩指令) | 解包命令加 `--no-debug-info`(三条流水线已统一,已修):baksmali 丢弃 `.line/.local` 调试指令 → 回编后 debug_info=0,dex 反而比原包小约 60KB。debug_info 仅用于断点调试/崩溃行号,发布加固包无运行时作用,剥离无功能损失。实测三包对比见 §9 变更历史 |
| **报错七:`LAUNCHER activity` 找到了但 `<类名>: missing`(sigcheck-only 步骤 4.5,多 dex 大包)** | c2daa1d(报错五修复提交)改写 `inject-loadlib.py` 时把 `SMALI_DIR_RE` 写成 `^smali(?:_classes\\d+)?$`(`\d` 多了一道转义)→ 正则只能匹配字面 `smali_classes\d+`,实际只命中裸 `smali/` 目录 → 类在 `smali_classes2…N/` 里的包(如简盒 21 个 dex)全部 `missing` | 正则改回 `^smali(?:_classes\d+)?$`(已修);教训:**`r''` 正则里 `\d` 写成 `\\d` 不报错、只是静默收窄匹配范围;改完必须用真实多 dex 包或 §7 冒烟验证。`mark-native.py` 复用同一正则,同受影响** |
| **产物"灰包":MT/文件管理器不认、无法安装(报错五)** | `apktool d -r --force-manifest` 解包 → 顶层 Manifest 被解码成文本 XML;`apktool b` 在 `-r` 模式下把文本 Manifest **原样拷回** APK、不重编成 AXML → 产物含纯文本 Manifest,PackageParser 直接拒识 | 解包命令**去掉 `--force-manifest`**(Manifest 全程保持二进制);配套:`inject-loadlib.py` 加 AXMLPrinter 分支、`extractNativeLibs` 改用 `patch-extractnativelibs.py` 二进制原位补丁。2.9.3 / 2.11.1 / `-f --force-all` 实测均无济于事,唯一修法是别让文本 Manifest 进入 build |
| ndk-build 报 JNI_OnLoad 重复 | 有人把 sig_check 改成了 JNI_OnLoad | 保持 `__attribute__((constructor))` 入口 |
| 机器上只编出 v7a | Application.mk 被覆盖/未生效 | 联合方案必须 `armeabi-v7a arm64-v8a` 双 ABI |
| 签名校验总是失败 | 重签 keystore 与指纹不一致 | sig_hash.h 指纹必须来自**最终发布** keystore |
| **字符串加密 0 命中(规则明明写了 `**`)** | 喂进去的是 `rules-to-filter.py` 的产物(dcc filter 正则)而非用户规则 → 二次转换产生畸形正则 `LLcom/myapp//[^.]*;...` | 直接把**用户规则文件**交给 `encrypt-strings.py`(它内部复用 `class_rule_to_filter`);见 §3.7.5 |
| **汇编桩报 `Invalid register: vNN. Must be between v0 and v15`** | 桩 `xor()` 的 `.registers` 过大(如 20)→ 参数 `p0` 落到 v18;`array-length`/`int-to-byte` 是 **12x 格式,上限 v15** | 桩的 `.registers` 收窄到 16(见 §3.7.4);注意该报错信息里的 `vNN` 与"行号"同时出现易误判为 apktool bug,实为真实校验 |
| **字段的字符串值仍是明文** | R8 把 `static final String = "x"` 折叠成 `.field ... = "x"`(注解形态),**不是** `const-string` 指令 | 本模块已含 field 分支(密文存桩字段 `C_F<idx>`,`<clinit>` 解密填值);若仍见明文,查该类是否被硬排除/`!` 排除 |
| **字符串加密报"没有任何字符串被加密"** | 规则只命中了系统类/依赖类(被硬排除),或规则格式错误未命中任何类 | 看日志里"被硬排除跳过: N 个类";确认规则是点号通配符格式且未写错包名;见 §3.7.5/§3.7.6 |
| **报错二十一:回编报 `Invalid register: vNN. Must be between v0 and v15` 或 `The maximum allowed register in this context is list of registers is v15` → `AndrolibException: Could not smali file`(字符串加密,含 Compose/Kotlin 大方法的包必现)** | 注入的 `invoke-static {dst}` 直接复用了原 `const-string` 的目标寄存器:const-string 是 21c 格式(8 位寄存器,v0–v255),而 invoke-* 是 35c 格式(**寄存器列表仅 4 位,上限 v15**)。被加密处寄存器编号 ≥16(大方法)或写成 `pN` 且换算后 ≥16(`.registers 20` 的单参 static 方法里 `p0 = v19`)即非法;smali 在解析阶段拒绝该文件 → 重打包失败。同文件的 `Cannot invoke "Object.hashCode()"` / `mismatched tree node` 是解析器被打断后的级联噪音,不是独立缺陷 | 注入形态改 `invoke-static/range {dst .. dst}`(单寄存器 range 恒连续、dex 中同为 3 个 code unit 无体积代价、对 v/p 都合法 → 无需换算 p→v);**同时**把幂等识别正则 `_PAYLOAD_RE` 的 `invoke-static ` 放宽为 `invoke-static(?:/range)? `(否则 `max_existing_idx` 对 `/range` 形态漏算 → 续跑 idx 从 0 重来 → keystream 复用)。诊断时先看 `[行,列]` 的列号是否落在 `{` 上,再用 `.registers 20` + `const-string v16`/`p0` 的最小 smali 喂 apktool 复现。见 §9 报错二十一 |
| **报错二十七:`StrDec.smali[94,0] mismatched input '' expecting END_METHOD_DIRECTIVE` → `Could not smali file`(回编阶段;此前桩注入日志已打印"字符串加密完成 / 桩=已注入")** | `encrypt-strings.py` 种子注入把桩骨架**腰斩**了:替换内联种子的范围越界(`m.end()` 是行尾不是行首,却写了 `keep_tail = skeleton[m.end():]`),骨架 193 行 → 92 行、方法中途 EOF,`write_stub` 再追加生成标记,产物行数 94 与报错行号逐字吻合。配套两个同批根因:① d8 为哨兵选的 `const-wide/high16` 只接受低 48 位全 0,真种子(任意 64 位)只换数字必报 `Invalid literal value … Low 48 bits must be zeroed out`;② 旧终检按"所有 `const-wide*` 行数"比较,骨架里 3 条无关 `const-wide/16` 恰好被截断吃掉 → 数量守恒退化成 `1 == 1` 通过,防线整体失效。**注意:本类缺陷的产物仍满足"含真种子/零哨兵残留"两条文本断言,照常绿** —— 必须靠结构断言(.method/.end method 成对)或真过 apktool 汇编才能拦 | `encrypt-strings.py`(非冻结)修:内联锚点**整行重建** + 替换范围严格止于行尾;`_fits()` **按新值重选指令变体**(装不下折回无后缀 `const-wide`);终检改为**同口径按值对比**并**新增"骨架行数守恒"硬断言**;测试补入 ⑧ 结构完整性 + ⑨ 真过 apktool 汇编闭环(apktool 缺失即明确 skip)。诊断时先 `wc -l` 对比 `stub-src/StrDec.smali`(193 行)与实际注入产物行数,再看 `.method`/`.end method` 是否成对。见 §9 报错二十七 |

---

## 6. AI 审查清单(逐条勾选,不许跳)

### 6.1 任何改动后的通用检查

- [ ] `bash -n scripts/*.sh` 全过
- [ ] `python3 -m py_compile scripts/*.py` 全过
- [ ] `git diff` 只含预期改动,无调试残留、无注释掉的死代码
- [ ] 未修改 `tools/dcc.zip` 包内容(dcc 是第三方工具,改动要单独说明理由;改包须重新 `zip` 并回归)

### 6.2 filter 相关改动

- [ ] 生成的 filter 行类名是**斜杠**格式(抽样 cat 确认)
- [ ] 无 `->` 出现
- [ ] `!<clinit|init>` 恒在首行
- [ ] 用 `MethodFilter` 真实逻辑跑用例(参照 §7 验证命令),不许只看肉眼看正则

### 6.3 native 化相关改动

- [ ] compiled_methods.txt 的解析正则与 §4.2 格式一致
- [ ] smali 目录匹配规则覆盖 `smali`、`smali_classes2`、`smali_classes3`…
- [ ] loadLibrary 插桩幂等(类里已有 loadLibrary 不重复插)
- [ ] exit 3(missing)在流水线中会失败——确认这是期望行为而非被吞掉

### 6.4 打包相关改动

- [ ] 产物未签名
- [ ] extractNativeLibs=true 已处理(且是唯一 Manifest 改动)
- [ ] 双 ABI so 都复制了

### 6.5 字符串加密相关改动

- [ ] 交给 `encrypt-strings.py` 的是**用户规则文件**,不是 dcc filter 产物(二次转换=0 命中)
- [ ] 系统类/依赖类硬排除名单仍生效(用 `**` 全量规则 + `android/app/*.smali` 冒烟验证)
- [ ] 指令改写用**原寄存器**(无 `.registers +1`),字段常量走桩字段引用
- [ ] 注入的 invoke 是 **`/range`** 形态(`{vN .. vN}`):寄存器编号 ≥16 时 35c 的 `{vN}` 必报 `Invalid register: vNN. Must be between v0 and v15`(报错二十一)
- [ ] `_PAYLOAD_RE` 同时认 `invoke-static {..}` 与 `invoke-static/range {.. .. ..}`(否则续跑 `max_existing_idx` 漏算 → idx 从 0 重来 → keystream 复用)
- [ ] 桩 `.registers ≤ 16`(否则 12x 指令 `array-length`/`int-to-byte` 报 Invalid register)
- [ ] 密钥流两侧对拍通过(§7 第 6 步,含 0/31/32/33 块边界)
- [ ] 幂等:重跑 0 条命中且文件逐字节不变;`clean` 能删掉残留桩与组件标记
- [ ] `--exclude-methods` 与 dex2c 联动生效(被抽走的方法保留明文)

---

## 7. 本地验证命令(AI 可直接复制执行)

```bash
ROOT=/path/to/apk-protect-action

# 1. 语法
for f in $ROOT/scripts/*.sh; do bash -n "$f"; done
for f in $ROOT/scripts/*.py; do python3 -m py_compile "$f"; done

# 2. 通配符 → filter(斜杠格式)
printf 'com.test.**\ncom.test.*\ncom.test*\n!com.test.libs.**\ncom.app.MainActivity\n' > /tmp/rules.txt
python3 $ROOT/scripts/filter/rules-to-filter.py /tmp/rules.txt /tmp/filter.txt
cat /tmp/filter.txt
# 期望:全部 Lcom/.../ 斜杠格式,首行 !<clinit|init>

# 3. 用 dcc 真实 MethodFilter 验证命中(关键!不许省)
# (dcc 自 tools/dcc.zip 解压,路径迁移后先 ensure_dcc 或 unzip 保证目录在)
unzip -qo $ROOT/tools/dcc.zip -d $ROOT/build/dcc
cd $ROOT/build/dcc/dcc
python3 -c "
import sys, logging; sys.path.insert(0, '.'); logging.disable(logging.CRITICAL)
import dcc as dm
f = dm.MethodFilter.__new__(dm.MethodFilter)
f._compile_filters, f._keep_filters, f._compile_full_match = [], [], set()
f.conflict_methods, f.native_methods = set(), set()
f._load_filter_configure('/tmp/filter.txt')
def should(fn):
    if fn in f._compile_full_match: return True
    if any(r.search(fn) for r in f._keep_filters): return False
    return any(r.search(fn) for r in f._compile_filters)
assert should('Lcom/app/MainActivity;onCreate(Landroid/os/Bundle;)V')
assert not should('Lcom/test/libs/Util;foo()V')
assert not should('Lcom/app/MainActivity;<init>()V')
print('OK')
"

# 4. mark-native 冒烟(自造最小 smali)
mkdir -p /tmp/smoketest/smali/com/demo
cat > /tmp/smoketest/smali/com/demo/A.smali <<'EOF'
.class public Lcom/demo/A;
.super Ljava/lang/Object;
.method public go()V
    .registers 1
    return-void
.end method
EOF
printf 'Lcom/demo/A;go()V\n' > /tmp/m.txt
python3 $ROOT/scripts/repack/mark-native.py /tmp/m.txt /tmp/smoketest
# 期望: go()V 变 .method native public go()V,且插入 loadLibrary <clinit>

# 5. 字符串加密冒烟(规则/硬排除/字段常量/多 dex/幂等/clean)
mkdir -p /tmp/se/smali/com/myapp /tmp/se/smali/android/app /tmp/se/smali_classes2/com/myapp
cat > /tmp/se/smali/com/myapp/A.smali <<'EOF'
.class public Lcom/myapp/A;
.super Ljava/lang/Object;
.field public static final K:Ljava/lang/String; = "field-secret"
.method public static m()Ljava/lang/String;
    .registers 1
    const-string v0, "instr-secret"
    return-object v0
.end method
EOF
cat > /tmp/se/smali/android/app/S.smali <<'EOF'
.class public Landroid/app/S;
.super Ljava/lang/Object;
.method public static s()Ljava/lang/String;
    .registers 1
    const-string v0, "system-never"
    return-object v0
.end method
EOF
printf '**\n' > /tmp/se/rules.txt
python3 $ROOT/scripts/string-enc/encrypt-strings.py /tmp/se /tmp/se/rules.txt \
  --salt-hex 000102030405060708090a0b0c0d0e0f
# 期望: A 的字段行保留明文初值(field-secret),<clinit> 新增 sget-object(桩 F000000)
#       + sput-object 回填链;m() 的字符串变 Base64 密文 +
#       invoke-static ...->d(...);android/app/S 的 system-never 必须原样保留
grep -c '"field-secret"' /tmp/se/smali/com/myapp/A.smali               # 1(初值保留)
grep -c 'sget-object v0, Lcom/nc/strdec/StrDec;->F000000' /tmp/se/smali/com/myapp/A.smali  # 1
grep -c 'system-never' /tmp/se/smali/android/app/S.smali # 必须 1
# 幂等:重跑不产生任何新改动
python3 $ROOT/scripts/string-enc/encrypt-strings.py /tmp/se /tmp/se/rules.txt \
  --salt-hex 000102030405060708090a0b0c0d0e0f              # 期望: 0 条 / 0 个类
# 清理:删除注入的桩
python3 $ROOT/scripts/string-enc/encrypt-strings.py clean /tmp/se

# 5b. 寄存器 ≥16 回归(报错二十一):大方法/Compose 的 const-string 目标寄存器可到
#     v16,p 寄存器换算后也可 >v15。注入必须是 /range 形态,否则回编报
#     "Invalid register: vNN. Must be between v0 and v15"(35c 上限 v15)
mkdir -p /tmp/se3/smali/com/myapp
cat > /tmp/se3/smali/com/myapp/Big.smali <<'EOF'
.class public Lcom/myapp/Big;
.super Ljava/lang/Object;
.method public static big(Ljava/lang/String;)Ljava/lang/String;
    .registers 20
    const-string p0, "big-reg-secret"
    return-object p0
.end method
EOF
printf '**\n' > /tmp/se3/rules.txt
python3 $ROOT/scripts/string-enc/encrypt-strings.py /tmp/se3 /tmp/se3/rules.txt \
  --salt-hex 000102030405060708090a0b0c0d0e0f
grep -c 'invoke-static/range {p0 .. p0}' /tmp/se3/smali/com/myapp/Big.smali  # 期望 1
# 续号不回归:删掉类标记重跑,旧类不二次加密、新类 idx 必须接在旧 idx 之后
# (幂等正则只认旧形态时 max_existing_idx 会漏算 → idx 从 0 复用)

# 6. 密钥流两侧对拍(构建期 Python vs 桩的 Java 逻辑)
python3 - <<'PY'
import importlib.util, base64, hashlib
spec = importlib.util.spec_from_file_location('es', 'scripts/string-enc/encrypt-strings.py')
es = importlib.util.module_from_spec(spec); spec.loader.exec_module(es)
salt = bytes(range(16))
def java_ref(payload_b64):          # 严格按 stub xor() 的公式重写
    raw = base64.b64decode(payload_b64); idx = int.from_bytes(raw[:3], 'big')
    return bytes(c ^ hashlib.sha256(salt + raw[:3] + bytes([i//32])).digest()[i%32]
                 for i, c in enumerate(raw[3:]))
for n in list(range(0, 81)) + [127, 128, 129, 255]:   # 块边界全覆盖
    plain = bytes((i * 7 + 3) & 0xFF for i in range(n))
    assert java_ref(es.encrypt_payload(salt, n % 1000, plain)) == plain, n
print('OK 密钥流对拍一致(含 0/31/32/33 块边界)')
PY

# 7. 桩 smali 语法(需要 java + apktool.jar,本地有)
#    把桩写成目录后汇编;若报 "Invalid register: vNN" → .registers 超过 16
java -jar $ROOT/build/dcc/dcc/tools/apktool.jar b -o /tmp/se.apk /tmp/se
# 期望: Smaling smali folder into classes.dex... 之后无 Invalid register 报错
```

> 完整链路(dcc 转译真实 APK → ndk-build → apktool 重打包)只能在
> GitHub Actions 上验证,本地无 NDK/apktool。每次改完必须触发一次
> workflow 才算闭环。

---

## 8. 已知边界与未做事项(审查时知悉,不算 bug)

1. **activity* 的识别依据**:Manifest `<activity>` 列表 + `*Activity;` 命名启发。
   非标准命名的 Activity 子类(如 `Lcom/a/Home;` 继承 Activity)不会命中——
   真正按继承链识别需要解析 dex,目前未做。
2. **dcc.py 静默吞错**:`dcc()` 异常只 log 不退出,所有"dcc 失败"都要靠
   工程包存在性检查兜底。给 dcc.py 本身加退出码属于改动第三方,暂不做。
3. **sig_check.c 的校验时机**:constructor 在 so 加载时执行,而 so 由
   `<clinit>` 的 loadLibrary 触发——即校验发生在**首个被抽取方法所在类
   初始化时**,早于该方法执行,满足方案要求;但不早于 App 其它未抽取代码
   (这是设计内的取舍,联合方案靠"抽取的是 LAUNCHER activity"保证足够早)。
4. ~~单独 dcc 的 Application.mk 还是 v7a 单 ABI~~:已统一——
   protect.sh 步骤 3 现与联合勾选一样覆写为
   `APP_ABI := armeabi-v7a arm64-v8a`(APP_PLATFORM 同为 android-21)。
5. **重签名指纹**:`sig_hash.h` 在流水线提取的是**上传时** APK 的指纹;
   开发者重签必须用同一 keystore。若 SaaS 场景要求用户上传 keystore,
   属于新需求,当前流程不支持。
6. **字符串加密 != 密码学安全**:salt 与密文同处一个 dex,懂格式者可依样解密。
   防的是 `strings` 一眼可见与静态特征扫描,不是有心人的定向逆向;
   真正的密钥材料必须走服务端/白盒方案,不要依赖本模块。
7. **字符串加密的覆盖边界**:注解值、Manifest/资源字符串、字段名/方法名/类名
   一律不加密(见 §3.7.1)。若用户期望"注解里的明文也消失",属新需求。
8. **字符串加密未做 dex 直改**:走 smali 改写 + apktool 回编,因此继承回编的
   全部保真风险(`--no-debug-info` 与 dex 版本 save/restore 已覆盖,见报错八/十八)。
   极端情况下若回编本身不可用,本模块也无法工作。

---

## 9. 变更历史(与本文相关的关键修复,审查上下文)

| 日期 | 修复 | 根因教训 |
|---|---|---|
| 2025-09 管道合并 | 三个总控(`protect-sigcheck.sh`/`protect-dcc.sh`/`protect-sigcheck-only.sh`)+ 编排器 `run-modules.sh` + `scripts/modules/` 薄包装 + 三个旧 yml → 合并为唯一总控 `scripts/pipelines/protect.sh`,唯一入口 `protect-modules.yml` | 三总控 90% 骨架重复(入口校验/NDK 工程/repack 流程),改一处公共行为要同步三处。合并后任何勾选组合只解包/回编 **1 次**(旧联合方案 2 次)。行为逐项对齐旧脚本:单独 sigcheck 走 inject-loadlib 双保险、联合走 mark-native 不插双保险、类名来源/步骤顺序不变;冻结模块(python/C)零改动。教训:**多入口多总控的重复骨架是行为漂移温床;公共流程下沉到一个总控,组合差异用勾选参数表达** |
| 报错一 | 补装 `asn1crypto` | dcc 内置 androguard 依赖不全 |
| 报错二 | IN_APK 绝对路径化 | `cd` 后相对路径失效 + dcc 静默吞错 |
| 报错二隐藏坑 | filter 去掉 `->` | full_name 无箭头,`''.join(triple)` 直接拼接 |
| 报错三 | `make-filter-from-apk.py` 的 DCC_DIR 补上第二级 `'..'` | 0f80c69 把脚本从 `scripts/` 移到 `scripts/filter/` 时,基于 `__file__` 的相对路径少改一级 → 云端 `ModuleNotFoundError: androguard`。教训:**移动脚本后必须重算所有基于 `__file__` 的相对路径**,并检查整个仓库有无同类问题 |
| 模块化 | 新增单独签名校验流水线(`protect-sigcheck-only.sh`/`.yml`) | 模块化第一步:签名校验与 dex2c 拆开,可单独测试定位问题。抽出可复用基础模块 `scripts/inject/inject-loadlib.py`(loadLibrary 插桩,幂等);so 名统一约定 `nc`,两模块共存时共享同一 libnc.so(constructor 与 JNI_OnLoad 天然兼容) |
| 报错四 | apktool 2.4.0→2.9.3 + 解包命令加 `--force-manifest` | `apktool d -r` 从不解码 Manifest(只原样拷贝二进制 AXML);sigcheck-only 的 `inject-loadlib.py --launcher` 首个消费解包目录文本 Manifest,正则匹配二进制必失败。教训:**依赖解包目录 Manifest 文本的代码,必须保证解包命令带 `--force-manifest`**;且 apktool 版本要跟得上新 aapt2 输出(2.4.0 强解 compileSdk 37 的包会把 android:name 解成空串,extractNativeLibs 的 sed 也会静默失效) |
| 报错五 | 解包命令去掉 `--force-manifest`;`inject-loadlib.py` 加 AXMLPrinter 分支;新增 `patch-extractnativelibs.py` 二进制原位补丁 | 报错四的修法引出了新问题:`--force-manifest` 解出的**文本 Manifest**,在 `apktool b -r` 时被原样拷回 APK(不重编成 AXML)→ 产物是 MT/系统都不认的"灰包"(本地实测 2.9.3 / 2.11.1 / `-f --force-all` 全都如此)。教训:**`-r` 模式下文本 Manifest 进 build 必出灰包;Manifest 必须全程保持二进制**,消费方一律改走 androguard/二进制补丁,不再依赖文本 Manifest。报错四与报错五是一对:前者要文本、后者要二进制,最终统一到二进制路线 |
| 报错六 | `protect-sigcheck-only.yml` 补 `pip3 install -r sigcheck/dex2c/dcc/requirements.txt` | sigcheck-only 流水线新增时漏抄 python 依赖步骤(另两条 workflow 都有)→ runner 无 lxml → AXMLPrinter 导入失败 → LAUNCHER 解析降级文本正则 → 二进制 Manifest 匹配失败。教训:**新建 workflow 必带依赖安装步骤;且"降级"路径在二进制 AXML 场景下等于必失败,排障先看降级警告行** |
| 报错九 | dcc.py 多 dex 支持(`auto_vm_all` + `compile_dex` 遍历全部 dex) | 云端联合方案 `no compiled methods`,本地单 dex 包复现不了。根因:dcc 原版 `auto_vm` 用 `get_dex()` 只读 classes.dex,LAUNCHER 在 classes2+ dex 的包必然 0 命中;dcc 静默 return 不报错,只能靠工程包存在性兜底。教训:**"本地能跑"要区分是代码对还是输入太幸运;多 dex 输入空间必须用伪造多 dex 包显式验证;用户提示"签名校验单独流水线已跑通"是关键定位线索——它不跑 dcc,直接把故障域缩小到 dcc 转译环节** |
| 报错八 | 三条流水线解包命令统一加 `--no-debug-info` | 用户发现注入后 APK 变大(4.39MB→4.77MB),对比定位:膨胀全在 dex `debug_info` 区块(63KB→1.86MB),R8 未被破坏(string/type/class 表逐项一致)。根因是 apktool 回编重建调试信息;修法 `d -r -f --no-debug-info`,实测注入后 dex 6,865,632 比原包还小 59,804 字节,method_ids +1(插桩)、class_defs 不变。教训:**baksmali/smali 往返不是无损的,调试信息是 release 包的纯负担;排"包变大"先对比 dex map 区块,别怀疑 R8** |
| 报错七 | `inject-loadlib.py` 的 `SMALI_DIR_RE` 修正 `\d`(c2daa1d 引入 `\\d` 双重转义) | 报错五修复提交改写该文件时把 `r''` 正则里的 `\d` 写成 `\\d` → 正则只命中裸 `smali/` 目录,`smali_classes2…N/` 里的类全部 `missing`(步骤 4.5 失败,多 dex 大包必现)。教训:**双重转义不报错、静默收窄匹配;`mark-native.py` 复用同一正则同受影响;改动后须用多 dex 包验证** |
| apktool 3.0.3 升级 | 仓库内 apktool.jar 2.9.3 → 3.0.3(官方 release) | 本地实测(简盒 2.1.0):`d -r` 解包 manifest 仍保持二进制 AXML("Copying raw AndroidManifest.xml"),注入/AXML 补丁/重打包全链路回归通过,产物非灰包。**升级 apktool 必须回归报错五红线:解包后 manifest 头 4 字节必须仍是 03 00 08 00** |
| **报错十三:`UnsatisfiedLinkError: No implementation found for MainActivity.onCreate`(联合方案,日志 docs/crash_20260908_113842.txt;签名一致仍闪退,与签名校验无关)** | 模块化改造引入的 `lib-ndk.sh` 统一 mk 模板只 wildcard 一级目录(`jni/*.c(pp)`),而联合方案里 dcc 产出的 `Dex2C.cpp`/`well_known_classes.cpp`/各方法 .cpp 与 sigcheck 的 `sig_check.c` 全在 `jni/nc/` 子目录 → 源码全部漏编 → 空壳 `libnc.so`,native 方法无实现 | `lib-ndk.sh` 的 `SOURCES` 增加第二级 `$(LOCAL_PATH)/nc/*.cpp/*.c`,`LOCAL_C_INCLUDES` 同步加 `$(LOCAL_PATH)/nc`。教训:**新 mk 模板必须覆盖所有实际源码布局;联合方案源码在 jni/ 与 jni/nc/ 两级,只收一级=空壳 so → UnsatisfiedLinkError(与报错十一同型:目录布局假设错→空壳)** |
| **报错十四:`apk not found (maps+data/app both failed)` → 签名一致仍延时 abort(联合方案,真机日志 com.xixin.box,Android 16/SDK 36)** | `find_apk_path` 两条路径在 Android 11+ 真机全灭:①maps 扫描只认 "base.apk+.so 同行"——那只在 extractNativeLibs=false 时出现,而本流水线强制 extractNativeLibs=true(so 解压到 lib/<abi>/ 加载,maps 无 base.apk 行);②兜底 opendir("/data/app") 被 SELinux 拒 → errno=13(EACCES)。结果:签名正确也走 `apk not found` → delayed_kill 延时 abort,与签名无关 | `find_apk_path` 新增分支(追加,不动原两分支):dladdr 取本 so 真实路径(失败则扫 maps 里 `/libnc.so` 行),截掉 `/lib/` 后段得安装根目录,拼 `/proc/self/root<根>/base.apk` 后 `access(R_OK)` 确认——`/proc/<pid>/root` 前缀绕开 maps 路径前缀不可读问题。教训:**extractNativeLibs=true 与 false 的 so 加载路径完全不同,maps 匹配必须两套布局都覆盖;定位 APK 不能只依赖可列目录(/data/app 在 Android 11+ 不可列),要用进程自身已打开/已映射资源反推** |
| sig_check.c 文件日志系统(用户 2026-09-08 需求,只追加不改写) | 真机"签名一致仍闪退"无 logcat 可查(无 adb 场景),无法定位校验走到哪个分支 | LOGD/LOGI/LOGE 重定义为 logcat+文件双写(所有调用点零改动);开关为 sdcard 根哨兵文件 `/storage/emulated/0/sigcheck_debug`(存在即开,运行时生效,无需重编译/重装);日志写 `/storage/emulated/0/Android/data/<包名>/files/sigcheck_log.txt`(MT 管理器可直接查看);文件 I/O 失败一律静默,不影响校验主流程;宿主测试用 `SIGCHECK_TEST_LOG` 环境变量替代 sdcard;注意 `siglog_detect` 引用的 `get_pkg_name` 需前向声明(否则 NDK clang 隐式声明报错) |
| **dcc 改为 zip 分发(用户 2026-09-09 需求)** | `sigcheck/dex2c/dcc/` 摊开目录(122 文件)整体收进 `tools/dcc.zip`(顶层即 `dcc/`,与 dpt-shell 形态一致),删除摊开目录与遗留 `sigcheck/dcc_filter.txt`。引用路径 6 处同步切换到解压产物 `build/dcc/dcc/`:`common.sh` 新增 `ensure_dcc`(幂等解压,fail-fast 校验 dcc.py 在位);`protect.sh` DCC_DIR 改为 `ensure_dcc` 填充;`lib-repack.sh` APKTOOL_JAR 指向解压产物(source 时先 ensure_dcc,因 sigcheck-only 勾选也要用 apktool);`inject-loadlib.py`/`make-filter-from-apk.py` 各自实现 python 侧幂等解压;`protect-modules.yml` 加解压步骤并改 pip 依赖路径 | 验证:bash -n/py_compile 全过;删旧目录后 ensure_dcc 幂等解压实测通过;make-filter-from-apk 用真实 APK 解析出 MainActivity(exit 0);AXMLPrinter/apktool 3.0.3 可用;protect.sh --dex2c 全链路 dcc 正常加载 21 个 dex。教训:**改分发形态必须全仓 grep 引用点(含 shell/python/yml 三种语言),bash 与 python 的解压兜底各写一份(不互相依赖执行顺序);解压后必须校验关键文件存在,不能只信 unzip 退出码** |
| **报错十五:云端 `unzip tools/dcc.zip -d build/dcc` 直接失败(checkdir: cannot create extraction directory: build/dcc, No such file or directory)** | `protect-modules.yml` 解压 dcc 的步骤在 `build/` 目录不存在时直接跑 unzip;`unzip` 的 checkdir 只能创建 zip 条目自身的最后一级目录,不会自建多级父目录,而 `build/` 不在仓库中(checkout 后不存在)→ exit 2,流水线在解压步骤就挂 | 解压步骤前补 `mkdir -p build`(幂等)。教训:**shell 工具的"自动建目录"假设要逐个验证——unzip 不等于 `mkdir -p` + 解压;`scripts/lib/common.sh` 的 `ensure_dcc` 因先 `mkdir -p "$dest"` 不受影响,只有 workflow 直跑 unzip 的这一处踩坑** |
| **报错十六:云端 NDK 编译失败 `incompatible integer to pointer conversion assigning to 'jobject' from 'int'`(dcc 生成的 `Java_Ee_v0_invokeSuspend__...cpp` 第 317/322/327 行 `v15 = 1;`,联合方案,日志 docs/报错日志.md)** | 三因素叠加:① dcc 代码生成器把 Kotlin 挂起函数 `invokeSuspend` 转译时,jobject 类型寄存器收到 `const/4 v15, 0x1` 整数赋值,生成的 C 类型不严谨(dcc 老毛病,一直存在,dcc 冻结区不动);② `setup-ndk` action 的"优先预装"候选逻辑**未做版本校验**——GitHub runner 把预装 NDK 升到 r27 后,`ANDROID_NDK_HOME` 抢先命中,`ndk-version: r26d` 参数被架空(日志实锤 `ndk/27.3.13750724`);③ r27 的 clang 18 把"整数赋指针"从 warning 升级为 error,老 clang(r26d)只警告能编过——所以"以前一直好好的" | `setup-ndk/action.yml`(非冻结)候选循环加版本校验:预装候选提取目录名主版本号(26.3.11579264→26),只认与 `ndk-version` 输入一致的;不匹配打印跳过行,全部不匹配走原有回退下载 r26d 路径。实测三场景通过(r27 抢位时正确跳过并选中 SDK 内 r26d / 只有 r27 时无命中触发回退 / r26d 预装直接命中零下载路径不回归)。教训:**"优先复用预装"类优化必须校验版本再采用——上游 runner 环境会变,信任候选≠信任版本;第三方生成代码的类型瑕疵遇编译器严格化是延迟引爆组合,"一直能编过"的包突然炸,先查"环境变了什么"再查"谁改了代码"** |
| 动态化 | 点号→斜杠 | dex 描述符是斜杠,Manifest 是点号 |
| 动态化 | mark-native.py | dcc 原版 --no-build 路径不 native 化、不插 loadLibrary |
| 动态化 | activity* grep 用 `[[:space:]]` | grep BRE 不认 `\s` |
| 动态化 | activity 展开剥壳 bug | 类列表是点号全名,`cls[1:-1]` 剥坏首尾 |
| **2026-10 三项(用户需求):dcc 规则优先修复 + 指纹存储混淆 + 环境检测模块** | ① `protect.sh` STEP2 类名来源重排:原逻辑"勾 sigcheck 即无条件抽主类",联合勾选时无视用户 `dex2c_rules` → 改为**规则优先**(有规则→rules-to-filter.py;无规则+勾 sigcheck→make-filter-from-apk.py 抽主类;无规则+未勾 sigcheck→fail-fast)。`--dex2c` 规则参数改为可选;workflow 联合勾选时 `dex2c_rules` 允许留空,规则输入改经 env 中转(堵 GitHub Actions 命令注入面)。② 指纹存储混淆:原 `sig_hash.h` 存明文证书指纹 `SIG_HASH`,so 里十六进制可搜(实测简盒包 armv7 libnc.so 偏移 0xcc8f 完整命中;arm64 被字面量池打散但运行时仍参与比对)→ 攻击者拿公开证书指纹原地替换即绕过。改为**双轮派生**:`make-sig-hash.sh` 生成 `SIG_SALT`(每次构建 os.urandom(16))+ `SIG_HASH_STORED`(`t1=SHA256(fp); stored[i]=t1[i]^salt[i%16]^(i*0x9E&0xFF)`),明文指纹不落盘;`sig_check.c`(冻结,用户当次点名)追加 `derive_expected`(约 20 行,复用文件内 sha256),`sig_verify` 仅替换全零判定行与比对块(仍恒定时间),其余零改动。③ 新增 `sigcheck/src/env_check.c`(全新文件,`--envcheck` 勾选):Frida(端口 27042/27043 回环探测 + maps 特征 + /proc/net/tcp LISTEN)、Xposed/LSPosed(已加载库特征 + 高置信路径)、调试器(TracerPid);命中 → 分离线程随机延时 abort(与 sig_check 同款);不检测 Root(用户拍板,避免误伤);零依赖 sig_check 符号 | 验证:bash -n 全过、workflow YAML 解析通过;指纹派生**三方对拍一致**(宿主 clang 编译 derive_expected 同逻辑 == 头文件 SIG_HASH_STORED == python hashlib 独立参照),负例(攻击者证书派生≠stored→延时 abort)通过,明文指纹确认不出现在 sig_hash.h;env_check.c 宿主端编译/链接/逐 probe 单测通过(干净环境五项探测全 0 无误报,被 ptrace 附着的 Termux shell 正确命中 tracer_probe)。**派生设计初版有坑:生成侧先写成 SHA256(fp‖salt) 而校验侧是 SHA256(fp),对拍立即暴露 → 统一为 SHA256(fp),salt 只参与 XOR 扩散;教训:两侧派生协议必须以对拍测试为准,不能只看注释自以为一致**。冻结模块红线:dcc/、extract-cert-fp.py、inject-loadlib.py、mark-native.py、apktool.jar 零改动(git status 复核) |
| **报错十七:签名一致但"open apk failed: errno=2" → 延时 abort 闪退(maps 分支,真机日志 docs/sigcheck_log.txt,包名 com.sdmnfowa.soad.p1)** | `find_apk_path` 的 maps 分支(`strstr("base.apk") && strstr(".so")`)被包名踩雷:①extractNativeLibs=true 时 so 从 lib/<abi>/libnc.so 加载,maps 里的 base.apk 行是 dex 映射,行内本不含 ".so";命中条件里的 ".so" 实际匹配到的是**包名中的 ".so" 子串**(`com.sdmnfowa.soad.p1` 含 `.soad`)→ 本不该入分支的行被误判命中——这解释了为何只有特定包名闪退,用户自研 app(包名无 ".so")一直正常;②命中后旧代码只截尾不截头,`*='\0'` 后 out 仍从行首开始 → out 是 "addr perms offset dev inode + 路径" 的整行前缀(真机日志实锤:LOGI 打出的"路径"就是整行)→ open 必 ENOENT → 签名一致也走 fail → delayed_kill。注意报错十四的修复(由 so 路径反推 base.apk)本身是对的,只是根本轮不到执行——maps 分支在它之前就误命中并 return 了 | maps 分支重写(用户当次点名报错日志):①命中条件去掉 ".so",凡 maps 出现 base.apk 行即取路径;②回溯最后一个空格取纯路径;③加 `access(R_OK)` 确认,失败继续扫行走原有 fallback(报错十四的 so-dir 反推分支不变)。宿主 clang 21 验证三场景:含 ".so" 包名的真机 maps 行→正确提取纯路径 access 命中(修复后 base.apk 本体存在,直接命中,无需 fallback);so 映射自 APK 的行→命中纯路径;候选不可读→继续 fallback 不判死。教训:**子串匹配要考虑"匹配到路径中非目标段"的可能(包名/目录名可含任意字符);maps 解析输出的路径必须验证其形态(纯路径)与可用性(access);把整行前缀当路径用这种错,一条 LOGI 实测日志就能暴露** |
| 中文全流程日志(用户 2026-11 需求):每行日志带中文说明,一眼分清成功/失败;同时不回退 so 加固 | 原 sigcheck_log.txt 只有零星英文缩写("nc: ok"/"nc: mm"/"v"),用户看不懂每行在干什么、也看不出哪个方法起作用;env_check.c 六项探测全程静默,extract_cert_der 零日志 | 新增 `scripts/sig-log/make-sig-log.py`:扫描 sig_check.c/env_check.c 源码中的 LSG 标记注释(`/*LSG:NAME|【成功】中文文案*/`),构建期生成 sig_log_data.h(XOR 密文数组+ID 枚举+解码函数,8 字节 key),protect.sh STEP3 在组装 NDK 工程前生成(fail-fast)。源码里明文文案就在调用点旁注释(人/AI 可读,注释即真相),so 内只有密文——`strings` 搜索中文关键词 0 命中,不回退 2026-10 路标词加固。sig_check.c 追加 `siglog_c()`+LSGD/LSGI/LSGE 宏(logcat+文件双写,五级前缀:【流程】正常走到某步/【成功】【通过】正常/【失败】出错走 fail 链/【数据】路径与指纹等中间值),find_apk_path 三条定位路、extract_cert_der 四步解析、sig_verify 主流程、delayed_kill 全部打点;env_check.c 独立实现 envlog(envlog_detect/envlog_c,遵守零交叉引用契约,哨兵与日志文件与 sig_check 相同),六项探测逐项输出 hit/clean。开关不变:sdcard 哨兵文件 `/storage/emulated/0/sigcheck_debug` | 验证:宿主 clang 联合编译两 .c 通过;真实 APK(动漫共和国)全链路日志实测——【流程】进入校验→【成功】打开/Signing Block magic/命中 v2 块(id=0x7109871a)/证书 DER 提取→【数据】实际指纹→【失败】指纹不一致→延时退出,每步可见;env_check 六项逐项输出,TracerPid 探测在 Termux 正确命中;`strings libnc` 密文验证通过;bash -n/py_compile 全过。教训:**日志可读性与防逆向可以兼得——文案编译期进密文、源码注释保明文,靠"生成器扫注释"而非"手写密文数组"(后者会重蹈报错十一/十四建复可读性的覆辙);日志打点要覆盖"方法是否存在/是否走到"的全部判定点,否则用户只知道结果不知道卡在哪** |
| **2026-10 so 加固(用户需求,防 PC 端逆向):编译参数加固 + 日志路标词中性化 + 延时堆破坏** | 防御评估结论:全部防线(签名校验/env_check/延时 abort)寄居在明文编译的 libnc.so,PC 端攻击者 `strings` 可见 "signature verify ok"/"env check triggered" 等路标词秒定位,Ghidra 将 sig_verify/env_check_run patch 一条 RET 即全灭(详见 docs/so加固报告.md)。三项低风险加固:① `lib-ndk.sh`(非冻结)mk 模板追加编译参数 `-fno-ident`(去编译器指纹)/`-Oz`(小函数内联模糊边界)/`-ffunction-sections -fdata-sections` + 链接 `-Wl,--gc-sections -Wl,--build-id=none`,全局自动生效,未来新增源码零维护;② `sig_check.c`/`env_check.c`(冻结,用户当次点名)日志路标词中性化:verify/mismatch/cert fp/env check → `nc: ok`/`nc: mm`/`nc: fp=`/`nc: t`,**排障日志(apk not found/errno/so-dir candidate)全部保留**(报错十四定位链路依赖),`strings` 不再泄露功能语义;③ `sig_check.c` delayed_kill 追加延时堆破坏:abort 前随机 8~23 轮 malloc(64~4KB)/free + 1~4ms 抖动,攻击者 hook 掉 abort 后堆布局已碎片化,后续崩溃难归因(纯 malloc/free 不写不触业务内存)。附带修复 2026-10 遗留缺陷:仓库模板 `sig_hash.h` 仍是旧明文 `SIG_HASH` 形态(无 SIG_SALT/SIG_HASH_STORED),本地直接编译 sig_check.c 必炸(宿主验证时暴露),已同步为派生形态全零占位 | 验证:宿主 clang 21 三场景运行时测试——全零槽位干净跳过(exit 0)/非零槽位无 APK 主线程存活→延时堆破坏后 abort(exit 134)/env_check 在被 ptrace 的 Termux 正确命中 tracer(TracerPid=20946,真实命中非误报);bash -n 全过;git diff 复核仅预期改动。**放弃字符串加密宏方案(用户拍板:不把清晰源码变天书)——纯 C 编译期加密会把源码可读性换掉,而删路标词达到同等"strings 盲区"效果;教训:加固手段的选择要以"源码可维护性"为约束,防 PC 的第一优先级是让自动定位手段(strings/模式匹配)失效,其次才是提高人工逆向成本**。冻结红线:dcc/、extract-cert-fp.py、inject-loadlib.py、mark-native.py、apktool.jar 零改动 |
| **报错十八:Compose 首帧 `IncompatibleClassChangeError: Found interface DrawScope, but class was expected` 闪退(联合方案,日志 docs/闪退日志.md,包名 com.sdmnfowa.soad.p1,签名校验全绿排除嫌疑)** | apktool/smali 3.0.9 回编不按 minSdk 选择 dex 版本,一律写 035;原包(minSdk=27)dex 为 038,内含接口 static/bridge 方法(Compose `DrawScope.drawRect-*$default`,flags=0x1009)——该布局仅 038+ 语义合法。逐 dex/逐方法/逐指令对比抽取包与原包实证:所有类 interface bit 零变化、方法级除目标两类的 native 化+`<clinit>` 注入外零差异、调用点指令零差异,**唯一全局差异就是 dex 头 038→035**;035 头使新 ART 按旧接口规则校验 → ICCE → 首帧绘制闪退。误排方向教训:先怀疑 dcc 误抽/filter 过宽,全量内容级对比后才锁定回编降级——**"工具回写不保真"要怀疑格式元数据(版本头/checksum/对齐),不只怀疑内容;ICCE 这种"两包内容一致却一个崩一个不崩"的案,先 diff 字节级元数据** | 新增 `scripts/repack/patch-dex-version.py`(非冻结,独立模块,CLI `<输入.apk> <输出.apk>`):androguard 读 Manifest minSdkVersion 推导目标版本(24+→037,26+→038,30+→039,否则 035),对 APK 内所有 dex 头第 4~7 字节原位回填。`lib-repack.sh` 的 `repack_build` 尾部追加调用(全部流水线共用,自动生效)。验证:未抽.apk(4 个 035 小 dex 回填,6 个 038 不动,精准无副作用);被降级抽取包(10 个 dex 全回填,逐字节对比仅头 '5'→'8' 1 字节差异,头外/非 dex 条目零改动)。**真机 ICCE 消失验证待用户装机确认**。**通用化修订(用户问"换 App/原包 037 怎么办"):单一 minSdk 推导对低 minSdk+高版本原包有盲区(R8 desugaring 产物可能依赖 038 接口语义,回填 035 修不好);v2 改为 save/restore 双模式——repack_unpack 后 `save` 记录各 dex 原始版本到 `<解包目录>/dex-versions.json`,repack_build 后 `restore` 逐 dex 恢复 `max(原版本, minSdk 推导下限)`(只升不降),原包 037 恢复 037、038 恢复 038、无记录兜底下限,对任意 App/minSdk/原包版本组合通用;教训:"按 Manifest 推导"只保证下限安全,信息在流水线中途会丢失的(回编降级抹掉原版本)必须在丢失前 save、恢复时 restore——元数据修复和内容修复一样要讲究信息保存时机** |
| **字符串加密模块(用户需求,dex 层,与 so 无关)** | 需求:加密源码里写死的字符串常量(URL/API key/日志文案),**范围用与 dcc 完全同一套规则**(手动输入类名 + 通配符/正则),且**即使用户写 `**` 全量也不能动系统类/依赖类**;字段的字符串值也要覆盖;不碰 so。实现:新增独立模块 `scripts/string-enc/encrypt-strings.py`(CLI `<解包目录> <规则文件> [--classes] [--exclude-methods] [--salt-hex]`,另有 `clean` 子命令)。① **选类**复用 `rules-to-filter.py` 的 `class_rule_to_filter`/`has_wildcard`/`expand_activity_keyword`(同一份实现);② **硬排除**前缀表(`android.`/`androidx.`/`java.`/`javax.`/`kotlin.`/`kotlinx.`/`dalvik.`/`com.android.`/`com.google.android.`/`org.apache.`/`org.json.`/`org.w3c.`/`org.xml.`/`sun.`/`org.jetbrains.`/`android.support.`),带点号前缀判定避免误伤 `androidXcustom`,规则层不可突破,`!` 排除优先;③ **指令改写**`const-string vX,"密文"` + `invoke-static {vX}, LStub;->d(...)` + `move-result-object vX`——**参数与结果都用 vX,不新增寄存器**(规避报错十一/十三式错位);④ **字段常量**单独分支:R8 把 `static final String = "x"` 折叠成 `.field ... = "x"`(**不经 const-string**),改为引用桩静态字段 `F<idx>`,由桩 `<clinit>` 运行期解密填值(首版漏了这个场景,本地用 `field ... = "字面量"` 用例才暴露);⑤ **算法** `cipher[i]=plain[i]^SHA256(salt‖idx_be24‖byte(i/32))[i%32]`,payload=Base64(idx_be24‖cipher),salt 16B 随机、重跑复用;⑥ **解密桩**`Lcom/nc/strdec/StrDec;` 纯 Java(d/dec/xor/`<clinit>`,ConcurrentHashMap 缓存 + `intern()` 保 `==` 语义,解密失败返回原串不抛),桩自身在硬排除内;⑦ **幂等**类内插标记注释 `# nc-strdec-encrypted` 整类跳过,另有 `stringenc-components.json` 组件标记(描述符+源文件路径,报错十八式"丢失前 save")供 `clean` 长期正确清理残留桩。接入:protect.sh 新增 `--stringenc 规则文件`(规则必填、fail-fast)、STEP5 内于 `mark-native.py` **之后**执行(被抽走的方法已是 native 壳自然扫不到,并用 `--exclude-methods compiled_methods.txt` 精确跳过)、每轮先 `clean`;workflow 新增 `stringenc` 勾选 + `string_rules` 输入(经 env 中转防注入)+ 规则文件生成 + 前置校验 + ARGS;执行顺序改为 sigcheck→dex2c→stringenc→packer。验证:py_compile/`bash -n`/YAML 解析全过;伪造 smali 冒烟(多 dex+系统类硬排除)14 项回归全绿;apktool 汇编桩零警告;密钥流两侧对拍长度 0..80+127/128/129/255 全过;真 dex 端到端(明文从字符串池消失、密文独立解密还原、androguard 读回字节码正确、字段与指令两类密文全对)。**踩坑记录:①最初把 `rules-to-filter.py` 的产物(dcc filter 正则)再喂给 encrypt-strings.py → 二次转换产生畸形正则 `LLcom/myapp//[^.]*;...` → 0 命中(与 §2 步骤 3 同型),改为直接传用户规则文件;②桩 `xor` 用 `.registers 20` → `p0` 落 v18,`array-length`/`int-to-byte` 是 12x 格式上限 v15 → 汇编器报 `Invalid register`,改 `.registers 16` 后零警告(一度误判为该报错是 apktool 误报,靠"改寄存器后警告跟着位移"才确认是真实校验);③`_FIELD_RE` 类型分组贪婪吞掉字段名首字符 → 输出丢字段名,改为以 `:` 锚定**。教训:**接入正则类模块时必须确认"输入是用户规则还是已转换产物"——二次转换不报错、只静默失配(与报错七的双重转义同型);跨语言(构建期 Python/运行期 Java)派生协议必须对拍,不能只靠注释** |
| **报错十九:字符串加密 `UnicodeEncodeError: 'utf-8' codec can't encode characters in position 15-16: surrogates not allowed`(encrypt-strings.py process_file → plain.encode('utf-8'),云端联合方案,日志 docs/报错日志.md)** | dex 字符串是 UTF-16 编码,非 BMP 字符(emoji 等)占两个 code unit;baksmali 按 code unit 逐个转义,smali 里输出 `\ud83d\ude00` 形态;`smali_unescape()` 对每个 `\uXXXX` 逐个 `chr()` 还原 → 得到的是 Python 里的**孤立 UTF-16 代理项**(未合并成单码点)→ `.encode('utf-8')` 禁止编码代理项直接炸,流水线在字符串加密步骤失败。宿主复现实锤:`\u0061\ud83d\ude00\u0062` unescape 后 repr 为 `'a\ud83d\ude00b'`,encode 即报同款错误。之前对拍测试覆盖了"Python 侧明文含 emoji 直接加密",没覆盖"baksmali 反编译产物中 emoji 以代理对形态出现"这条真实路径 | `smali_unescape()`(非冻结脚本)修:①合法代理对(高代理 `\\uD800–\\uDBFF` 紧跟低代理 `\\uDC00–\\uDFFF`)合并成单码点 `chr(0x10000 + ((hi-0xD800)<<10) + (lo-0xDC00))`,emoji 的 UTF-8 字节正常进密文,运行期 Java 桩 `new String(bytes, UTF_8)` 无损还原;②孤立高代理/孤立低代理/低前高后/高代理后接 BMP 转义等非法形态抛 `ValueError`,走调用方既有的"跳过该条字符串"安全路径(该条保留明文,不阻塞构建)。回归 11 项全绿(emoji 合并/连续 emoji/BMP 不回归/8 种非法形态安全拒绝)。教训:**UTF-16 与 Python str 之间不能逐 code unit 直译——代理对必须成对合并;对拍测试的输入要用"上游工具的真实输出形态"(baksmali 转义产物),不能只用源语言明文;解密桩侧(Java UTF-16 语义)天然无此问题,恰是 Python 构建期独有** |
| **报错二十:字段常量场景运行期 NPE(外部 AI 审查发现,宿主静态实锤;联合方案/含 R8 折叠 static final String 的任意包)** | `STUB_TEMPLATE` 的 `<clinit>` 把 `%CLINIT_FILL%`(字段解密填充,内含首次 `d()` 调用)放在 CACHE 初始化**之前**;`d()` 入口 `sget CACHE` + `get()` 又在 try 块外 → 类含 `.field ... = "明文"`(R8 必折叠)时,该类 `<clinit>` 执行 → `d()` 读 null CACHE → NPE → `ExceptionInInitializerError` 闪退。之前"字段与指令两类密文全对"是 androguard 静态读回验证,非运行时验证,NPE 只有真 JVM/真机才暴露 | 六项完整性修复(全部在非冻结的 `encrypt-strings.py`):① CACHE 初始化挪到 `%CLINIT_FILL%` 前(P0);② `max_existing_idx` 补 `_FIELD_REF_RE` 扫 `.field ... = StrDec;->F\d+:` 形态,续跑字段 idx 计入(防 keystream 复用);③ `process_file` 改索引循环,密文 const-string 行前瞻下一行为 `invoke-static ...->d(` 即跳过——类标记被外部删掉也不二次加密;④ 复用桩且有新字段时 `read_existing_stub_fields` 读回旧字段合并重写桩(同名密文不一致 fail-fast),不再静默丢字段;⑤ `clean_components` 删桩前扫描:存在仍引用桩的已加密类(CLASS_MARK + `->d(`/`->F\d+:`)则 fail-fast 拒绝,杜绝半加密目录;⑥ 删死函数 `field_to_java_code`、修正 classify 优先级注释。回归中额外揪出并修复既有隐藏 bug:`hits==0` 早退分支 `return next_idx` 丢弃续出的 idx → 同样导致 keystream 复用,改 `max(next_idx, idx)`。宿主回归 17 项全绿(clinit 顺序含无字段场景/双形态 idx/前瞻防重密/半加密 fail-fast/字段合并/报错十九不回归)。教训:**静态字节码校验 ≠ 运行时验证——桩的 `<clinit>` 这种"初始化顺序敏感"代码必须按 JVM clinit 语义推演或真机跑;外部 AI 审查的价值在"另一个读代码的视角",但其结论也要逐条实跑复核(本次 7 条全部属实,无一误报)** |
| **报错二十一:回编 `AndrolibException: Could not smali file` + `Invalid register: v19. Must be between v0 and v15` / `The maximum allowed register in this context is list of registers is v15`(联合方案首次在真实大包上跑字符串加密,日志 docs/build.log,`AboutScreenKt.smali[1691,19]` 与 `GitHubDispatcher.smali[1242,4]`)** | `encrypt-strings.py` 注入 `invoke-static {dst}` 时直接复用了原 `const-string` 的目标寄存器:const-string(21c)寄存器 8 位(v0–v255),而 invoke-*(35c)寄存器列表仅 4 位(**上限 v15**)→ 寄存器编号 ≥16(Compose/Kotlin 大方法)或写成 `pN` 换算后 ≥16(`.registers 20` + 单参 static 方法里 `p0 = v19`,与日志 `v19` 逐字吻合)时注入指令非法,smali 在解析阶段直接拒绝整个文件 → 重打包失败;同文件后续 `Cannot invoke "Object.hashCode()"`/`mismatched tree node` 是解析器被打断后的级联噪音。之前只冒烟了 `.registers 1`/`v0` 的小用例(寄存器远小于 16),既没覆盖"真实工具产物形态",也没覆盖"大寄存器方法",直到 21 dex 含 Compose 的包才暴露 | 注入形态统一改 `invoke-static/range {dst .. dst}`(单寄存器 range 恒连续、dex 中同为 3 个 code unit 无体积代价、对 v/p 两种写法都合法 → 不做 p→v 换算,规避 `.registers`/`.locals` + long/double 双宽的解析成本);**并同步它的消费者**:`_PAYLOAD_RE` 由 `invoke-static \{` 放宽为 `invoke-static(?:/range)? \{`,否则 `max_existing_idx` 对 `/range` 形态命中 0 → 续跑 idx 从 0 重来 → keystream 复用(报错二十式)。宿主回归(apktool 3.0.3 实跑汇编):`.registers 20` + `const-string v16` / `const-string p0` 两用例改写后汇编通过(改前同用例精确复现 `T.smali[11,4] Invalid register: v19`);幂等重跑 0 条;删类标记场景下旧类不二次加密、新类 idx 续号为 2(旧正则该场景命中 0 → idx 回 0);§7 冒烟(v0 小寄存器 / 硬排除 / 字段常量)不回归。教训:**把寄存器从一个指令的操作数位置搬到"格式不同的另一条指令"时,必须同时校验目标格式的寄存器编码宽度——"不新增寄存器"只解决了数量问题,没解决宽度问题;模块产物形态一变,所有以该形态做识别的消费者(幂等正则/续号扫描)必须同批更新,否则静默失配(与报错七的双重转义、报错十一的目录布局同型:错了但安静)** |
| **报错二十二:字符串加密产物"闪退但无 FATAL 日志"(联合方案,含任意被加密字符串的包;日志缺失正是本 bug 的特征)** | `STUB_TEMPLATE` 的 `xor([BI)[B` 内层循环用 `v14`/`v15` 当异或临时寄存器,而该桩声明 `.registers 16`、形参仅 `[B`+`I` 两个 ⇒ **参数固定落在末尾寄存器 `p0=v14`、`p1=v15`**(smali 规则:参数占最后 N 个)。于是 `aget-byte v14, p0, v13` 实为 `aget-byte v14, v14, v13`:第 1 轮把数组引用 `p0` 覆盖成刚读出的字节(int),第 2 轮起把该 int 当数组引用解引用。ART 解释器 `aget-byte` 只在引用为 `nullptr` 时抛 NPE,非 null 非法引用直接按数组指针解引用 ⇒ **SIGSEGV 原生崩溃(Java 层 catch 不住,`d()` 的 `.catch Throwable` 兜底也无效)**,发生在 `<clinit>` 解密 `loadLibrary("nc")` 的密文时 → 任何 ≥2 字节的字符串都会让 so 从未 dlopen、`sig_check.c`/`env_check.c` 的 constructor 不执行,**闪退且 logcat 无 `FATAL EXCEPTION`(段错误不走 ART uncaught-exception 路径),也无自研文件日志(挂在未加载的 constructor 里)**——本条 bug 的"无日志"正是它区别于普通 Java 闪退的指纹。历史盲区:此前只对拍"算法两侧等价"(Python 加密 ↔ Java 解密)与"静态字节码读回",均通过——算法确实对、寄存器用法确实错,静态验证覆盖不到"寄存器编号语义" | `encrypt-strings.py`(非冻结)修:① `xor` 内层循环临时寄存器 `v14`/`v15` → **`v7`/`v8`**(外层循环里 v7/v8 只做 `MessageDigest.update` 的临时值,进入内层时已死值、每轮 `goto :loop_outer` 必重新赋值,内层期间无人读 → 安全槽位);**不动 `.registers 16`**(维持 12x 的 v15 上限约束,报错二十一)。② 新增**构建期机制防线**(教训:"文字警告防不住,只有机制能"):`_render_stub()` 统一展开模板并调 `_check_stub_registers()` —— 解析每个方法的 `.registers`+形参得到参数寄存器区间,断言**任何写指令的目标寄存器不得落入该区间**(`pN` 形态或 `vN≥param_lo` 均拦),违反即 fail-fast;`write_stub` 改走 `_render_stub`,杜绝绕过。自检显式跳过 `#` 注释行,避免说明文案里的 "aget-byte v14" 被当真实指令误伤。宿主回归 20 项全绿:①修复后模板过自检;②把修复回退成 v14/v15 → 自检精确拦截;③误报边界(invoke 用 pN 参数/局部寄存器 v7v8 不拦,写参数从严拦);④寄存器语义模拟器按修复后指令逐条执行,还原 0/1/2/12/21/26/79 字节明文(含中文 UTF-8)全对,**不只是"不崩"而是结果正确**;⑤端到端(加密类+字段常量 → 写桩)桩内真实指令无 v14/v15 目标、用 v7/v8、字段声明齐全;⑥寄存器宽度校验(12x 上限 v15、invoke 35c 上限 v15)全过。教训:**"两侧算法对拍"证明不了"寄存器分配正确"——寄存器是运行期状态,必须在语义层(指令逐条执行成本/覆盖)或真机验证;桩这类"手写汇编"资产,参数寄存器区间是硬约束,应做成构建期断言而非注释提醒** |
| 日志开关统一改名(用户 2026-xx 需求):加固 so 的哨兵文件由 `sigcheck_debug` 改为 `xixin_debug` | 用户希望"整个 App 的闪退/日志"只记一个开关名:jian_box(被加固的普通 App)也做了同款哨兵开关的崩溃日志落盘,两个系统各记一个文件名会给真机排障添乱 | `sig_check.c` 的 `#define SIGLOG_SENTINEL` 与 `env_check.c` 的 `access()` 哨兵路径同步改为 `/storage/emulated/0/xixin_debug`(env_check.c 仍与 sig_check 同一个文件,注释注明);`SIGCHECK_TEST_LOG` 宿主测试开关不变。校验:两文件开关路径逐字节一致、旧名源码零残留(仅注释保留改名说明)。**jian_box 侧**(独立仓库,纯 Kotlin,不引入 native):新增 `utils/DebugSwitch.kt`(`isOn` 实时探测哨兵文件、异常静默按关;`allowFileLogging(debugBuild)=debugBuild||isOn`),`AppLogger.init()` 与 `CrashHandler.saveCrashToFile()` 的落盘条件由写死的 `BuildConfig.DEBUG` 改为 `DebugSwitch.allowFileLogging(BuildConfig.DEBUG)` → release 包只要建 `/storage/emulated/0/xixin_debug` 就落盘崩溃日志与运行日志,不建则维持"零日志"。教训:**多系统共用的"开关契约"(文件名/路径/存在即开语义)要在设计期就统一,否则用户要在两台机器/两个 App 间记两套名字;开关做成"运行时探测的外部哨兵文件"而非"编译期常量",才能在 release 真机上零重装开启排障** |
| 第三方崩溃捕获库 xCrash 的加固兼容(用户 2026-xx 需求:给被加固 App 加"闪退必留日志") | jian_box(纯 Kotlin 被加固 App)引入 `com.iqiyi.xcrash:xcrash-android-lib:3.1.0`(MIT,AAR 约 420KB,纯本地写 tombstone,无上传/无权限)以捕获 **native 崩溃 + ANR**——这是 Java 层 `setDefaultUncaughtExceptionHandler` 机制性抓不到的部分(本次闪退即 ART SIGSEGV;Java 崩溃仍由 CrashHandler 负责,故 xCrash 侧 `javacrashHandlerEnable=false` 避免两个 handler 互相覆盖)。引入第三方 native 库后必须评估与本加固流程的兼容性 | 逐条核查加固流程对第三方 so/类的影响:① `repack_install_so` 是"覆盖式 cp 单文件",只拷 `libnc.so`、不清理 `lib/<abi>/`,故 `libxcrash.so` 与 `libnc.so` **共存不被误删**;② `mark-native.py` 只处理 dex2c 抽出的方法(用户规则文件决定),不写 xcrash 就不会被 native 化;③ **字符串加密命中风险(本次唯一真实风险)** → `encrypt-strings.py` 的 `SYSTEM_CLASS_PREFIXES` 追加 `'xcrash.'`(硬排除,规则层不可突破),理由:xCrash 的 Java↔native 靠 JNI 回调绑定,xcrash 包的类/方法名或字符串被加密/改写会导致捕获失效或自身崩溃,属"框架依赖"(与 `kotlin.`/`androidx.` 同型)。jian_box 侧配套:R8 需 `-keep class xcrash.** { *; }`(JNI 方法名不可混淆)、`attachBaseContext` 初始化(最早期,早于 onCreate)、开关沿用 `/storage/emulated/0/xixin_debug`(与加固 so 日志系统同一个哨兵文件,用户只记一个)、DebugSwitch 增 `isOnAtStartup`(带重试,应对 attachBaseContext 早期 sdcard/FUSE 未就绪)。验证:`is_system_class('xcrash.XCrash')=True`/`('xcrashSomething.Foo')=False`(不误伤同类前缀)等 8 例全对;`py_compile` 通过。教训:**引入第三方 native 库到加固产物前,必须三查——它的 so 会不会被加固流程误删(目录清理/覆盖式安装)、它的类会不会被字符串加密/dex2c 误伤(JNI 绑定类必须硬排除)、它的 native 符号会不会被 R8 混淆(必须 keep);"能共存"要在流程代码里核实,不能想当然** |
| **报错二十三:只勾 stringenc 报 `scripts/lib/lib-ndk.sh: line 24: .../project/jni/Application.mk: No such file or directory`(exit 1,与勾选顺序/加密顺序无关)** | `--stringenc` 加入时,STEP 3/4(组装 NDK 工程 + ndk-build)与 STEP 5 的 `repack_install_so`/`repack_force_extractnativelibs` 均为无条件执行;但 stringenc 是纯 dex 层模块(流程文档 §3.7 定位"与 so 完全无关"),只勾它时 sigcheck/dex2c/envcheck 的 `mkdir -p .../jni` 分支全部跳过 → `ndk_write_mk` 往从未创建的 `$WORK/project/jni/` 里 `cat > Application.mk` → bash 直接报 No such file or directory 退出。此前所有实测组合都含 sigcheck 或 dex2c,jni/ 恰好总被创建,缺陷被掩盖 | `protect.sh`(非冻结)修:① STEP 3/4 整体包进 `if [[ $WANT_SIGCHECK -eq 1 \|\| $WANT_DEX2C -eq 1 \|\| $WANT_ENVCHECK -eq 1 ]]`(有 so 需求的组合才组装/编译);② `repack_install_so` + `repack_force_extractnativelibs` 同条件包进(纯 dex 组合没有 libnc.so,manifest 无需 extractNativeLibs 改写,原样安装只会 cp 空/不存在的目录;extractNativeLibs patch 对无 lib/ 包也是无意义改写)。验证:mock 外部命令端到端干跑,stringenc-only 组合 STEP 3/4 整体跳过、manifest patch 不执行、exit 0;sigcheck-only 组合 STEP 3/4 照常执行、`ndk_write_mk`/`ndk-build`/`repack_install_so` 路径不变(guard 未误伤)。教训:**新增"纯 dex 层"模块接入多模块总控时,必须逐段检查总控里所有 so 相关步骤(NDK 组装/编译/so 安装/manifest so 属性)对该组合是否成立——此前组合"恰好总含 sigcheck"掩盖了无条件步骤,排障时应验证最小勾选组合,不能只测增量组合** |
| **报错二十四:单独勾 stringenc(无 dcc/无 sigcheck)产物 App 秒闪退且 logcat 无任何日志(简盒 `简盒_signed_01_02_14_sign.apk`,21 dex,规则 `com.xixin.box.*`;闪退发生在首个被加密类加载的 `<clinit>` 阶段,先于一切 Java 代码执行)** | `encrypt-strings.py` 字段常量分支把 R8 折叠的 `static final String` 初值改写成 `.field ... = Lcom/nc/strdec/StrDec;->FNNNNNN:Ljava/lang/String;` 引用形态(设计本意"桩 <clinit> 解密填值"),但 **dex 规范不允许 FIELD 型(0x19) encoded static_value**——apktool 内置 smali 编译器宽容放行照编,产物 dex 里该字段初值变成 FIELD 型引用,ART 类初始化在执行任何字节码之前校验 static_value 即失败 → 秒闪退且不走 uncaught-exception 路径,无 FATAL 日志。androguard 全包扫描实锤:21 dex 恰好 34 处 FIELD 型 static_value,全部指向桩字段 `Lcom/nc/strdec/StrDec;->F\d+`,与桩内 34 个字段常量一一对应(受害字段:`UnifiedApiClient.GATEWAY`/`BuildConfig.APPLICATION_ID` 等);其余环节(加密算法对拍 13600 条、idx 唯一性、寄存器合法性、规则命中、注解零命中)逐项验证无恙 | `encrypt-strings.py`(非冻结)修:① 字段行**保留原明文初值不动**(产物形态恢复合法),加密改由**回填**实现——新增 `inject_clinit_backfills()` 在字段所属类 `<clinit>` 的 `return-void` 前注入 `sget-object vX, StrDec;->FNNNNNN` + `sput-object vX, <owner>;-><field>`(运行期用解密结果覆盖字面量初值;与桩自身填 F 字段同一套合法形态;无 `<clinit>` 则新建 `.locals 1`,已有且 `.locals 0` 抬到 1);② 消费者同步(报错二十一教训):`_FIELD_REF_RE` 由 `= StrDec;->F\d+:` 改认 `<clinit>` 回填引用 `sget-object … ->F\d+:`(clean 半加密探测/`max_existing_idx` 续号两处消费者同批切换);③ **构建期机制防线**(报错二十二教训"只有机制能防"):新增 `_ILLEGAL_FIELD_INIT_RE` 硬断言,每次跑完复读全包,出现任何 `.field … = StrDec;->F…` 即 fail-fast。验证:§7 冒烟全绿(字段常量/硬排除/幂等重跑 md5 一致/寄存器≥16/删标记续号 F000001/多 dex);**真汇编→读回闭环**:apktool 3.0.3 实编产物 + androguard 读回,FIELD 型(0x19) 从 34 处降为 0 处,字段初值恢复 STRING 型,回填链在 dex 层为合法 `<clinit>` 指令;非法旧形态注入测试被断言精确拦截(exit 1)。遗留:`.registers 0` 的已有 `<clinit>` 抬到 1 只做行内 `0→1` 首处替换,极端格式可能没改到(真机验证兜底)。教训:**"汇编器宽容放行 ≠ 产物合法"——smali 是宽容编译器,dex/ART 才是裁决者;产物必须用 androguard 读回结构层验证,不能只看汇编不报错;凡"运行期才求值"的需求,只有 `<clinit>` 指令这一条合法路径,字段初值区是纯静态数据** |
| **报错二十五:只勾 stringenc 回编失败 `StrDec.smali[18,4] no viable alternative at input 'sget-object'`(docs/构建报错日志.md,云端 CI,apktool 3.0.3 Smaling 阶段)** | `STUB_TEMPLATE` 字段区性能优化注释(报错二十修复时写的 `<clinit>` 顺序说明)里出现了字面占位符 `%CLINIT_FILL%`;`_render_stub()` 用无上下文的 `str.replace()` 展开模板,把注释里的占位符也替换成了真实回填指令 → 指令被注入**类体顶层(非 .method 区)**,第 17 行注释被拦腰截断成 `# 再执行 `、第 18 行变成裸指令 `    sget-object v0, …`、第 19 行残留半句中文注释,smali 解析器在类体顶层遇到指令即报 `no viable alternative at input 'sget-object'`,整个重打包失败。触发条件 = 桩 `<clinit>` 需要回填字段(即包内存在 R8 折叠的 static final String);注释行占位符是报错二十改动时顺手把说明文案从 `<clinit>` 内部挪进字段区注释时引入的,历次"渲染后目测/汇编小样本"恰好都是回填非空路径与文案检查分离,未覆盖"注释含占位符"形态 | `encrypt-strings.py`(非冻结)修:① 注释文案改写为纯文字(不含占位符字面量);② **三道机制防线**(报错二十二/二十四"只有机制能防"教训)加进 `_render_stub()`:防线1 模板注释行含任何占位符即 fail-fast;防线2 渲染后残留任何占位符(漏配/多写 `.replace`)即 fail-fast;防线3 渲染产物类体顶层(非 `.method..end method` 区)出现裸指令即 fail-fast(兜底拦住任何把指令注入非方法区的回归)。验证:apktool 3.0.3 真实汇编闭环——把占位符注回注释行复现旧缺陷,得到与报错日志**逐字一致**的 `[18,4] no viable alternative at input 'sget-object'`(注释截断/裸指令/残留半句三特征全对);修复后桩(含 F000001/F000002 双字段回填)真汇编通过产出 apk;防线对旧形态注入精确拦截。教训:**模板占位符是"全文替换"语义,注释也是被替换文本——占位符字面量绝不允许出现在模板注释里(要引用就拆开写);`replace()` 型模板展开必须配"替换前后都有断言"的机制防线,靠 review 目测注释防不住这种一字符级失误(与报错七的双重转义同型:错了但安静,只在下游炸)** |
| **报错二十六:整个 App 卡在系统启动页无法进入(明文不崩不报错,logcat 无 FATAL;规则 `com.xixin.box.*` 产物 `简盒_signed_08_15_29_sign.apk`,21 dex;第三方工具加密的同款包正常)** | **不是加密错,是解密太慢把启动首帧卡死了**。v1 桩每条字符串解密 = `Base64.decode` + 按 32 字节分块算 SHA-256(每块 5 次 `MessageDigest.update` + 1 次 `digest`)+ `new String` + `intern()` + `ConcurrentHashMap` 缓存查表;全包 13015 条密文 ≈ 23733 次 digest + 约 11.9 万次 update。放大器是包本身 `debuggable=true` → ART 全程解释执行,JIT/AOT 不优化,同代码慢 10~50 倍 → 启动关键路径(Compose 首帧 + `Application`/`MainActivity`)的解密风暴串行阻塞首帧,表现为"停在系统启动页"而非崩溃。第三方方案每类自带一张 `short[]` 表 + 一个 `$()` 方法,解密=纯数组 XOR(纳秒级),故无感。旁证:第三方实际加密 10273 个类(含 androidx/kotlin),比本工具 874 类多 12 倍却正常 → 排除"规则/加密范围"因素;13015 条密文逐条解密 100% 成功、idx 无重复、桩完整、`<clinit>` 回填链 34 个目标全存在,逐项排除"加密正确性"因素 | `scripts/string-enc/` 全模块由 v1 重构为 **v2/S2**(实现见 `docs/字符串加密v2重构方案.md`):① **算法换血**——密钥不再运行期 SHA-256 派生,改为**只存 1 个 8 字节种子**,密钥流用 splitmix64 **O(1) 现算**(`keyByte(seed,i) = mix64(seed + (i>>>3)*GAMMA) >>> ((i&7)*8) & 0xFF`),解密退化为 `Base64 + 纯 XOR`;**零表**——第三方每类塞一张 `short[]`,本方案连表都不要,全包仅 1 个解密方法 + 1 个 long 字段,比第三方更轻;② **密文形态**由 `Base64(idx_be24‖cipher)` 改 `Base64(offset_be24‖cipher)`,全包按序分配**不重叠**区段;③ **编译链闭环**(决策 D2):`stub-src/StrDec.java` 是唯一真相,`build-stub.sh`(javac → d8 → baksmali)自动产出 smali 骨架,**人永不手写 smali**——直接回应报错二十一/二十二/二十五"手写 smali 改一次炸一次";④ **种子注入**改为**双锚点一致性注入**并配机制防线:`SEED` 字段初值 + d8 内联到使用点的常量(`const-wide` 各变体)必须**同时**改写且数量守恒,fail-fast 拦截哨兵残留(实测 d8 行为:种子非 0 时保留 `.field … SEED:J = 0x…L` **并**把种子内联到每个使用点——只改 `.field` 行会导致运行期仍用旧种子,这是本轮最险的坑);⑤ **报错二十四防线原样保留**:字段初值仍是明文字面量,值经 `<clinit>` 回填(决策 D4),`_ILLEGAL_FIELD_INIT_RE` 构建期硬断言在位;⑥ **顺手修真 bug**:v1/S1 草稿 `d()` 用 `char[] out` + `new String(char[])`,语义是 Latin-1,中文/emoji 多字节 UTF-8 必乱码 → 改 `byte[]` + `new String(out, "UTF-8")`。**离线验证全绿**:A1 双端对拍 288 例 0 失败(Python `key_byte` ↔ 桌面 JVM 反射调用真实桩 `keyByte/d`,`tests/test-keystream-crosscheck.sh`;含 4 种子 × 256 字节密钥流全扫、非 8 对齐 offset、跨 64 位块边界、空串/中文/emoji 代理对/全角/边界码点/5188B 最长串);A2 冒烟 19 项全绿(加密形态/空串跳过/字段明文初值+`<clinit>` 回填/桩零哨兵残留/无 `.line`/幂等重跑产物字节一致/种子复用/冲突种子被已有桩覆盖);A3 续跑 + A4 结构全绿(`tests/test-structure-resume.sh`,**真闭环**:从加密脚本真实产物抽取密文,用同种子编译的桩逐条解密回原文——第一轮 4/4、两轮合并后 5/5;两轮 offset 零重叠;产物无 FIELD 型 static_value、桩密钥材料只有 `SEED`)。教训:**"功能对"≠"能上线"——性能也是正确性的一部分,且只有量级分析(单条成本 × 全包量 × 解释执行放大倍数)能预判,靠小样本冒烟永远测不出来;"比第三方更轻"要在方案层兑现(第三方省不掉的表,本方案直接不存在),而不是靠优化已有表;凡依赖编译期优化的注入(d8 内联常量),必须用**双锚点 + 数量守恒**做机制防线,断言"注入会让编译器保留它"是想当然(本轮实测推翻);历史报错的防线(报错二十一/二十二/二十四/二十五)在重构里必须逐条迁移验证,不能因为"架构换了"就默认失效;离线闭环测试(真产物抽密文 → 真桩解回)比"两侧算法各自对拍"更能抓 offset 分配类缺陷** |
| **报错二十七:云端 CI 回编报 `StrDec.smali[94,0] mismatched input '' expecting END_METHOD_DIRECTIVE` → `Could not smali file`(docs/build.log;只勾 stringenc,21 dex,规则 `com.xixin.box.*`,种子 `16e59aefff97adb1`;桩阶段已打印 `✅ 字符串加密完成: 12786 条 / 873 个类 (seed=…, 桩=已注入)`,失败发生在之后的 apktool Smaling 阶段)** | **`encrypt-strings.py` 的种子注入把桩骨架腰斩了**——三个根因同批,缺一都不会这么隐蔽:① **主因 · 替换范围越界**:`inject_stub_seed()` 里 `keep_tail = '' if m.group('tail') is None else skeleton[m.end():]` 把"丢弃 d8 生成的十进制浮点注释"写成了"丢弃行尾之后的全部文本"——`m.end()` 是**行尾**不是行首;而骨架里恰好只有这一条内联锚点带注释(`# 1.854068899003674E149`),于是每次注入都必然把 193 行骨架截到 92 行(`d()` 的循环体 + `keyByte`/`mix64` 两个方法全丢),方法中途 EOF → smali 解析器报 `expecting END_METHOD_DIRECTIVE`;`write_stub` 再追加 `# nc-strdec-gen v2` 生成标记,产物 94 行,**与报错行号 94 逐字吻合**。② **同批必炸 · 指令变体装不下新值**:d8 为哨兵 `0x5eed000000000000` 选的是 `const-wide/high16`(该形态**只接受低 48 位全 0** 的字面量),而真种子是任意 64 位随机数 → 只换数字必然非法(实测 `Invalid literal value: 1649895194077146545. Low 48 bits must be zeroed out`),**100% 复现而非概率**;故只修①会立刻撞上②,两处必须同批改。③ **为什么静默进产物 · 终检口径自相抵消**:原终检比的是"所有 `const-wide*` 行数"而非"值等于哨兵的锚点数",骨架里另有 3 条无关的 `const-wide/16`(`0x7`/`0x8`/`0xff`)——**恰好被①一并吃掉**,数量比较退化成 `1 == 1` 照样通过,报错二十六新建的那套"双锚点 + 数量守恒"机制防线在本场景整体失效,缺陷一路走到 apktool 阶段才炸(现场离根因很远)。另:本地 `test-encrypt-smoke.sh` 当时 19 项全绿也拦不住——它只做文本断言("含真种子""零哨兵残留"),而**腰斩后的产物照样满足这两条** | `encrypt-strings.py`(非冻结)修:① 内联锚点改为**整行重建**,替换范围严格止于行尾(`skeleton[:m.start('indent')] + line + skeleton[m.end():]`,行尾旧浮点注释随该段一起丢弃,无需单独判断);② 新增 `_fits(variant, value)`,**按新值重选指令变体**——`/high16` 要求 `value & 0x0000FFFFFFFFFFFF == 0`、`/16` 要求有符号 16 位可编码、无后缀恒真,装不下即折回无后缀 `const-wide`(可编码任意 64 位);③ 终检口径改为**同口径按值对比**("值等于哨兵的锚点数" == "值等于真种子的锚点数",不再把 GAMMA 等无关常量算进来)+ **新增"骨架行数守恒"硬断言**(直接拦住替换范围越界把文件吞掉的形态,是报错二十二/二十四/二十五"只有机制能防"教训的又一次兑现);④ 回归测试补入 **⑧ 结构完整性**(`.method`/`.end method` 成对、S2 方法数齐全、无 `const-wide/high16` 承载高位非零种子的非法形态)+ **⑨ 真过 apktool 汇编闭环**(apktool 缺失时明确 skip,不伪绿)。**验证**:① 修复后同种子注入 193 行守恒,apktool 3.0.3 真汇编通过,`apktool d` 回读 dex 确认 `.field … SEED:J` 与内联常量均为真种子、变体已折叠为 `const-wide`;② 端到端(加密 → 汇编)产物 apk 生成成功;③ 拿 `git show HEAD:…` 的**未修复代码**对同一骨架注入,精确复现 92 行腰斩 + `[92,45] mismatched input '' expecting END_METHOD_DIRECTIVE`,且**旧测试的文本断言在旧产物上依然全绿**(坐实盲区)、**新断言在旧产物上精确失败**(`.method=2` / `.end method=1` 不成对)→ 证明新防线不是摆设;④ 全套回归:A1 密钥流对拍 288 例 0 失败,A2 冒烟 22 项全绿,A3 续跑 + A4 结构全绿。教训:**"替换文本的一小段"必须用"段落边界"表达,而不是"某个 offset 之后的全部"——`m.end()` 是行尾还是行首、锚点是字面量还是整行,差一个字符就从"改一行"变成"删掉整个文件的后半部分";凡新增构建期防线,必须用"旧缺陷样本能否被拦住"反向验证一遍,否则防线口径可能与被防的缺陷恰好互相抵消(本次数量守恒终检就是活例:它没漏判值,是判错了口径)** |
