目录下两个apk是对比


（切记！MT的对比会支持以下功能
忽略编译优化
忽略寄存器数量
忽略调试信息
忽略nop指令）

然后忽略这些之后作者实测对比:

发现多了类似于以下寄存器代码
const/4 v3, 0x0
move v3, v3

但是一个整个类里面也只有几个

这种完全防不了对比，这种只能防对比类名

也就是说如果对方知道哪些是比较重要的类然后进去一看发现只有方法开头插入了一些无用寄存器且只有部分方法有，然后它就能轻松找到我修改的部分能懂吗？
    
    
    
    